## Module: 10-evals-observability.md

### Criteria Results
| # | Criterion                        | Verdict |
|---|----------------------------------|---------|
| 1 | ASCII System Topology            | ✅  |
| 2 | Token Cost Economics & SLA        | ✅  |
| 3 | NFR Trade-offs                   | ✅  |
| 4 | Distributed Resilience           | ✅  |
| 5 | Enterprise Security Boundaries   | ✅  |
| 6 | Design Case Studies              | ✅  |

### Deficiencies (if any)
None. All six criteria are met at the cited sections/line ranges below.

**1. ASCII System Topology — PASS**
- Diagram present with box-drawing characters (`┌─┐│└─┘├┤┬┴┼─`) at §1.1 lines 49–132 (not tables-only; plane table at lines 137–144 and score-owner table at lines 149–154 are accompanying).
- Planes mapped in the diagram: **Control** (lines 57–83: Edge SSO/`cid`/`run_id`, Policy PII redact before judge or golden ingest, DATASET pin tag/`as_of`/split list, EXPERIMENT target×scorers/`num_rep`/`max_conc` immutable, CI GATE hard EM/schema fail-closed + soft Δ, EVAL LEDGER GoldenItem + `evaluator_version` + McNemar, Circuit ONE per JUDGE provider ≠ agent, Fallback primary judge → cross-family → heuristic, SIGTERM/drain flush OTLP / do not block TTFT), **Data** eval/judge sidecar + prod tracing (lines 88–100: code oracles EM/schema/`refund≤cap`/hidden tests; process tool F1/step SR; product DB goal-state/τ-bench; LLM-as-judge G-Eval/RAGAS/swap-and-tie; Phoenix `suppress_tracing()`; app emits OTLP once; Collector → LangSmith AND Tempo; online eval sample 0.1 async; 100% metrics / sampled content; 25k runs/trace hard reject), **Tool proxies** (lines 103–117: Zero-Trust wrap, RFC 8707 `aud`, tenant NEVER from tool args, CI sim τ/Docker NO `refund.execute` vs prod MCP audience-bound, judge MCP read traces only, allowlist URLs), **Persistence** (lines 103–119: GOLDEN dataset tag/`as_of`/split indefinite, EXPERIMENT immutable scores WORM + JSON export, TRACE STORE 14d/180d, CALIBRATION κ slice + human queue, token vault not in spans, `hide_inputs`), **Telemetry** (lines 121–132: WORM cid/`example_id`/`eval_ver`/`prompt_hash`/score/(b,c)/`dataset_tag`/`hide_inputs`; metrics coverage %/`sample_rate`/p50/p95/p99 [policy]/time-to-score/breaker per judge; traces OTel `gen_ai.*` Collector fan-out OpenInference EVALUATOR in own project; usage agent tok + judge tok + platform units/spend cap remaining).
- Request-flow narrative is §1.2 lines 158–181 (production path 1–7: ingress token-bound ids → DLP-redact before export → inner loop OTLP once → Collector fan-out dual-write → keep-policy under load → online sidecar off path → halt+100% metrics/coverage %; offline CI 8–13: pin `(dataset_id, tag, as_of, splits)` → simulator MCP target → code oracles then soft → gate `reportRun`/jq/pytest → JSON export new `run_id` → promote via annotation queue never train on CI tag; canary 14: shadow 1–5% coverage-matched vs control). Interview talking point line 183.

**2. Token Cost Economics & SLA — PASS**
- Explicit `$ per 1k` with assumptions at §3.1 lines 316–354 (stated judge: Claude Sonnet 4.6 **$3 / $15** per MTok from 01; **2,000 in / 200 out**, no cache → **$9 / 1k**; cache 1,800-token rubric prefix at 0.1× → **~$1.1 / 1k** input-side; RAG CI loop: agent ~4k/800 with 70% cache read on 3k prefix + two RAGAS-style judges + LangSmith extended). Table (lines 328–334): LangSmith extended **$5 / 1k**; base **$0.50 / 1k**; Langfuse Core included **$0** incremental / overage **[inferred] $0.80**; Braintrust Pro **$1.50 / 1k** + GB. All-in RAG (lines 338–343): agent **≈ $13.6** + judges **$18** + platform **$5** = **~$37 / 1k**. Pattern table (lines 347–354): `num_repetitions=3` **~$97 / 1k**; 100% online @ 100 QPS **$86k–$864k/day** — do not. List-price assumptions restated at lines 310–312; Why-It-Matters preview lines 19.
- Latency tiers with mitigations at §3.3 lines 369–393. ⚠️ No vendor `evaluate()` p50/p95/p99 (lines 19, 310, 371). **[inferred policy]** (lines 382–393): **p50** user chat unaffected (judge never on path; OTLP async flush); **p95** user chat same as **04/07** (`hide_inputs` at SDK, not a second LLM); **p99** user chat fail-closed on hop/`$` caps (**04**) — sync judge on p99 is an architecture bug; **p50** time-to-score **< 30–60 s** (cache rubric prefix; dedicated eval project; T=0 structured out); **p95** time-to-score **< 3–5 min** (per-example timeout; majority-of-3 only on gate set); **p99** time-to-score shed to heuristic + page coverage (judge-provider breaker → schema/regex fallback; do not block deploy on sidecar p99 except CI); **p50** CI 1k conc. 8 dominated by target p50 × ceil(N/conc); **p99** CI bound with per-example timeout + ingest 429 backoff (smoke `--sample 20` PR; full nightly).
- Throughput and back-pressure at §3.4 lines 397–416: online sample LangSmith **0.1** / high-volume **0.01–0.05**; 100 QPS × 5% = **5 judge/s ≈ 432k/day**; coverage SLO **~1,000 scored / window**; Plus ingest **500k events/h**, **5.0 GB/h**, **5k POST/min**; Phoenix queue **20,000** overflow `RESOURCE_EXHAUSTED` → Kafka `partition_traces_by_id`; CI `max_concurrency` default **0 = serial**; spend cap pauses evaluator. Admit user request iff **agent** breaker closed — ignore judge breaker; shed content sample → keep 100% metrics + errors → pause soft judges → keep code oracles; size judge in-flight \(S p\) and OTLP bytes, not just user QPS; Collector `memory_limiter` first.

**3. NFR Trade-offs — PASS**
- Availability, RPO, RTO, compliance stated with working targets at §3.5 lines 418–428 (availability **99.9% control plane [policy]** for CI API + Collector; prod chat **must not** depend on LangSmith/Langfuse; judge sidecar fail-open; **RPO** goldens=last tagged version object-store copy, experiments=exported JSON, traces=accepted by Collector (in-flight SDK batch gone on crash — WAL if prove-what-agent-did), scores=WORM append; **RTO** CI=replay pinned tag + prompt SHA minutes, prod tracing=Collector failover / second exporter seconds, judge=heuristic fallback immediate / backfill hours Langfuse 25k cap; compliance judges=subprocessors ZDR per analysis task, PCI LangSmith prohibits cardholder data, health/finance goldens → Phoenix self-host or Braintrust hybrid, GDPR datasets indefinite = time bomb if promoted PII, audit logs writes ~70+ not reads of traces).
- Competing-NFR tensions in the same table: SaaS eval outage vs self-host Phoenix / Hybrid in-VPC; treating SaaS UI as RPO=0 vs promoting traces then deleting the project; fast degraded scores vs bit-identical G-Eval vs backfill **costs** judge tokens; online sample vs offline pin (coverage-matched canary); playground public share leaks examples **and** feedback; cost vs latency (judge **[inferred] $9 / 1k**, RAG CI **~$37 / 1k**, 5% of 100 QPS **$4.3k–$43k/day**; sync judge **adds** p99; async adds **$**; 100% online vs 1% missing the incident; extended-retention auto-upgrade); coverage vs spend cap (trip cap → scores stop → looks stable). Consistency (line 425): experiment immutable; unpinned `as_of` inconsistent by construction.

**4. Distributed Resilience — PASS**
- Durable execution: Temporal / Kafka **equivalent** at §4.1 lines 434–444 (LLM/graders/sandboxes = Temporal **Activities** not Workflow; `MaximumAttempts` on paid judge calls; `non_retryable_error_types` for 400/auth/policy; Workflow = dataset V + experiment E + prompt SHA P; Activities = target, code scorer, judge, upload; Braintrust × Temporal `start_to_close_timeout` **300s**; do **not** put LLM-as-judge inside the **production** workflow — signal + separate worker pool). Durable state (line 440): persist dataset tag, prompt commit, model id, `run_id`, per-example output URI, per-scorer `{key,score,comment}`, `evaluator_version`; JSON export so deleted SaaS project does not erase gate evidence; new `run_id` every candidate. Kafka/outbox (line 442): `eval.intents`, `eval.scores` (WORM), `eval.dlq`, `otel.spans` (`partition_traces_by_id`); compaction on `example_id`; full log = chain-of-custody; WAL + checkpoint DB + SIEM independent of experiment store.
- Failure taxonomy at §4.2 lines 462–474: **transient** (408/429/5xx/529, TLS reset, judge timeout, OTLP 429 — full jitter, honor `Retry-After` iff \(0<t\le 60\), same idempotency `run_id`+`example_id`, do not retry 400/RBAC) / **permanent** (400 schema, 401/403, κ<0.60, contamination_risk=high, `aud` miss — fail job CI / skip score online; do not failover a 400 to an uncalibrated second judge) / **poison pill (judge hack)** JudgeDeceiver / **poison pill (jailbreak of the judge)** / **poison pill (dataset)** / **poison pill (gate)** / silent quality drop / trace sampling miss / confused deputy. Idempotency: `run_id`+`example_id` (line 466); new `run_id` for every candidate (line 440). Encoded in `TransientError`/`PermanentError`/`retry_with_jitter` (§5 lines 631–712).
- Circuit breaker closed → open → half-open at §4.2 lines 476–490 (one breaker per **judge provider** plus per **trace ingest** backend, **distinct from** the agent/model breaker; do not open solely on 429-with-Retry-After; do not open the **agent** breaker because the **judge** 429d; half-open probe is a **cheap** structured 1-token classify, not a 48k-criterion HealthBench item), including ASCII state diagram lines 478–490. Encoded in `BreakerStateMachine` (§5 lines 651–685).
- Fallback chain: primary judge (cross-family from the generator) → secondary **other-vendor** judge (same rubric, new `evaluator_version` suffix) → **deterministic heuristic** (`schema_ok`, EM slots, refund cap, toxicity regex, step-budget); **never** invent a 1–5 rubric integer from a heuristic and store it as `g_eval`; `PermanentError` on κ-fail / contamination / RBAC **does not** failover to “ask the agent to grade itself” (line 492). Encoded in `heuristic_judge` (§5 lines 814–815).

**5. Enterprise Security Boundaries — PASS**
- Zero-Trust MCP at §4.3 lines 494–516: eval harnesses **are** MCP/tool clients; CI bot with production refund tool is a confused deputy; separate IdP clients; audience-bound tokens per MCP server; allowlist MCP URLs; **no production write APIs on eval MCP**; tool allowlist on agent under test; identity (`tenant_id`) from example metadata is a **label**, not authorization for prod tools; short-lived per-job credentials; least privilege on **every** eval `tools/call`; Temporal resume **re-RBAC**. Topology Zero-Trust box (lines 105–111). Encoded in `mint_eval_token` (no passthrough, no `refund.execute`) / `GATEWAY_AUD` (§5 lines 572–581, 910–921).
- Tool-level RBAC / least privilege at §4.3 lines 498–516: `datasets:read/update/delete/share/download/clone`; resource tags `Contains-PII=true` + **deny** ABAC; tag atomically at `POST /datasets`; `datasets:clone` checked on both source and target; Fleet MCP `mcp-servers:invoke` separate from trace read; Langfuse org/project roles; Braintrust RBAC on Pro+. Encoded in `EVAL_TOOLS` (`ci_runner` sim only, `judge` `read_trace` only, prod writes absent) and `authorize_dataset` ABAC deny-share PII (§5 lines 577–581, 924–938).
- PII pipeline detect → redact → audit at §4.4 lines 518–529 (ingest **and** before judge **and** before dataset promote; deterministic regex + NER; judges receive **already-redacted** text; PCI: no PAN in spans **or** goldens). Topology Policy “PII redact BEFORE judge or golden ingest” (lines 61–65); request-flow step 2 (line 163). Encoded in `redact_pii` / `hide_inputs` (§5 lines 715–730).
- Immutable telemetry / chain-of-custody at §4.4 lines 531–535 (WORM append-only: timestamp, `correlation_id`, `example_id`, `trace_id`, `span_id`, `evaluator_id`, `evaluator_version`, `prompt_hash`, model, params, key, score, `comment_hash`, `dataset_id`, `dataset_tag`, split, `hide_inputs=true`, `sample_rate`; provider traces 14–180d are **not** a SIEM). Kafka full log **is** the audit (line 442). Topology `Audit (WORM)` (§1.1 lines 123–131). Encoded in `WormScore` (§5 lines 890–897).

**6. Design Case Studies — PASS**
- Exactly two scenarios in §6 (header at line 1178: “Exactly two enterprise designs”).
- **Scenario 1** (§6 lines 1180–1236): RAG golden-set CI gate, SOC2 B2B copilot, 1k nightly / 100–300 PR smoke, judge-only **[inferred] $9 / 1k**, all-in RAG CI **~$37 / 1k**, `num_repetitions=3` **~$97 / 1k**; ASCII architecture lines 1186–1221; 3-way matrix (A/B/C) across cost, latency, ops complexity, security, scalability lines 1225–1234; rationale recommends **B** (pinned `golden-rag-v12`, dual oracles nDCG+EM+faithfulness, McNemar/bootstrap vs −2 pp, 5% online + coverage SLO, Collector dual-write) line 1236.
- **Scenario 2** (§6 lines 1237–1291): agent trajectory eval + Langfuse production tracing, 20 QPS × ~12 obs ≈ **240 obs/s ≈ 21M/day** / **~22M units/day**, process vs product keys, eval SA cannot `refund.execute`; ASCII architecture lines 1243–1277; 3-way matrix lines 1281–1289; rationale recommends **B** (τ/CRM outcome plus process F1, Langfuse sessions, OTel Collector dual-write, 5–10% judge / 100% code args, simulator in CI) line 1291.

### STATUS: APPROVED
