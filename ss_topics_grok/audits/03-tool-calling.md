## Module: 03-tool-calling.md

### Criteria Results
| # | Criterion                        | Verdict |
|---|----------------------------------|---------|
| 1 | ASCII System Topology            | ✅  |
| 2 | Token Cost Economics & SLA        | ✅  |
| 3 | NFR Trade-offs                   | ✅  |
| 4 | Distributed Resilience           | ✅  |
| 5 | Enterprise Security Boundaries   | ✅  |
| 6 | Design Case Studies              | ✅  |

### Deficiencies (if any)
None. All six criteria are met at the cited sections/line ranges below.

**1. ASCII System Topology — PASS**
- Diagram present with box-drawing characters (`┌─┐│└─┘├┤┬┴┼─`) at §1.1 lines 44–137 (not tables-only; plane table at lines 141–148 is accompanying).
- Planes mapped in the diagram: **Control** (lines 51–83: edge auth/RPM/breaker, policy PII+tool RBAC, schema compiler, per-turn allowlist, MCP discovery, Temporal orchestrator, idempotency store, circuit breaker, fallback compile), **Data** generation + executor (lines 88–99: tokenizer → prefill → constrained decode → parser; complete-JSON execute-gate, Pydantic `extra=forbid`, bulkhead Semaphore; hosted/server-tool inversion noted), **Tool proxies** (lines 104–116: Zero-Trust RFC 8707, no token passthrough, Stripe adapter same-key-after-500, CRM/MCP `tools/call` with SSRF filter), **Persistence** (lines 104–121: Postgres saga/thread/HITL/Temporal history/outbox, Redis-or-Stripe idempotency including 500s, MCP catalog hashes, soft prompt-cache KV / grammar ≤24h), **Telemetry** (lines 123–136: WORM audit, metrics p50/p95/p99, traces, usage on terminal event).
- Request-flow narrative is §1.2 lines 152–165 (12-step ingress → policy+identity → schema compile → allowlist without prefix mutation → MCP discovery → dispatch model → parse+ID contract → execute-gate → downstream+completeness → semantic vs HTTP recovery → saga/webhook → emit+audit).

**2. Token Cost Economics & SLA — PASS**
- Explicit `$ per 1k` with assumptions at §3.1 lines 338–357. Formula \(C = n \cdot (T_{\mathrm{miss}} P_{\mathrm{miss}} + T_{\mathrm{hit}} P_{\mathrm{hit}} + T_{\mathrm{write}} P_{\mathrm{write}} + T_{\mathrm{out}} P_{\mathrm{out}}) / 10^{6}\) (lines 334–336); \(T_{\mathrm{out}}\) includes thinking. Assumptions: Sonnet 5, 20-tool agent (~2,000 schema + 354 hidden + 1,500 system + 500 user, 800 output); a **tool-using question** = 1 planning model-call + 2 parallel REST tools + 1 synthesis; no thinking; 5m cache on tools+system prefix (lines 342–344). Table (lines 346–353): cache-warm **$19.14 / 1k questions**; +20% one-shot schema retry **$21.06**; uncached **$33.42**; computer-use uncached **$35.78** / cached **$17.98**. Schema-invalid increment **$0.0048** → **+$0.96 / 1k** at 20% (line 355).
- Latency tiers with mitigations at §3.2 lines 374–380: **p50** wall (tool-using turn) **< 3 s** (stream planning; `asyncio.gather` independent reads; REST tool p95 **<800 ms**; cache tool prefix); **p95** wall **< 8 s** (model-call p95 1–8 s; do not promise 8 s for N=20 sequential computer-use); **p99** hang fail-closed on idle gap / Activity timeout (nested timeouts ordered; inter-event watchdog; one retry owner; circuit-break identical GET pagination; `tool_choice: "none"` on closing turn). Documented ceilings at lines 365–372 (OpenAI 600 s / 5 s connect; vLLM first FSM “several seconds”; Temporal StartToClose 30 s).
- Throughput and back-pressure at §3.3 lines 384–404: Stripe dashboard-specific request limits vs OpenAI TPM leftover; MCP AgentCore **30 tools/page**; OpenAI **>~15 req/min per `prompt_cache_key`**; vLLM `--max-num-seqs`. **[inferred] bulkhead** 100 agents × 4 parallel = **400** in-flight HTTP; Semaphore per downstream class (payments / CRM / MCP / sandbox) (lines 397). Back-pressure: admit iff LLM breaker ∈ {closed, half-open} **and** tool-class breaker **and** both semaphores have room; Stripe 429 sleeps that bulkhead only; shed (disable parallel writes → HITL mutating → deterministic decline JSON); cap rounds (`DEFAULT_MAX_TURNS=10`); loop detector on `(tool, args_hash)` (lines 399–404).

**3. NFR Trade-offs — PASS**
- Availability, RPO, RTO, compliance stated with working targets at §3.4 lines 407–416 (availability **99.9% gateway** + tool-class bulkheads + multi-vendor 503/529 fallback, never failover a 400 schema; **RPO=0** for irreversible tools via idempotency+saga+`batch_id` checkpoint before execute, KV/grammar minutes–24 h best-effort; **RTO** interactive LLM failover **< 1 s**, in-flight PaymentIntent retry **same** key; compliance regional +10% / Anthropic 1.1× US geo, ZDR excludes batches/files, redact before inject, Stripe: no emails as idempotency keys).
- Competing-NFR tensions in the same table: failover vs output-distribution drift / schema mapping; treating KV as RPO=0; fast failover vs bit-identical tokens (T>0) vs Stripe replay vs “create again”; exactly-once as a lie without the store (Activities at-least-once); residency vs latency vs DLP on cached tool results; cost vs latency (cached **$19.14/1k** vs uncached **$33.42** vs **+$0.96** schema-fail vs computer-use **$35.78/1k**); cache hit rate vs `list_changed` rug-pull freshness.

**4. Distributed Resilience — PASS**
- Durable execution: Temporal (every LLM call and tool I/O is an Activity; Workflow = deterministic loop; `workflow-id = tenant:thread_id`; `max_retries=0` in SDK; checkpoint `call_id`/`tool_use_id` + saga_state before ack; DLQ) and Kafka (`tool.intents` outbox before side effect, `tool.results`, `stripe.webhooks` → Signal Workflow, `tool.dlq`; compaction on `intent_id`; poison skip) at §4.1 lines 424–441.
- Failure taxonomy at §4.3 lines 455–462: **transient** / **permanent** / **poison pill** / **semantic** / **partial side effect** / **hallucinated name**. Idempotency at §4.2 lines 443–451 (Stripe-style store status+body including 500s, ≥24 h, same key after 500; key from `(tenant, tool_name, canonical_args_hash, user_intent_id)` or `workflowRunId + activityId`; never model-invented UUID). Poison-pill loop detector on identical `(tool, error_type)` (lines 275, 432, 459).
- Circuit breaker closed → open → half-open at §4.3 lines 464–478 (one breaker per (provider, model) **and** per tool-class; do not trip on 429-with-Retry-After; half-open probe is a **cheap read**, not `create_payment_intent`), including ASCII state diagram lines 467–478. Encoded in `CircuitBreaker` / offline test (§5 lines 597–650, 1145–1162).
- Fallback chain: primary (Sonnet 5 / GPT-5.4 strict tools) → secondary vendor (same compiled IR) → **deterministic** schema-valid decline (`status: "degraded"`, no charge); tool-class open → queue/HITL, not a second processor; PermanentError on schema does **not** failover (lines 480). Encoded in `FallbackChain` / `deterministic_decline` (§5 lines 978–1015).

**5. Enterprise Security Boundaries — PASS**
- Zero-Trust MCP at §4.4 lines 484–491: remote servers as OAuth 2.1 resource servers; RFC 9728 metadata; **RFC 8707** `resource` indicator; PKCE; MUST validate audience; MUST NOT token-passthrough; per-client consent; RFC 8693 token exchange at gateway; pin MCP manifests against rug pulls; DNS-rebinding CVE-2025-66414 / 66416 with pin TS ≥1.24.0 / Python ≥1.23.0.
- Tool-level RBAC / least privilege at §4.5 lines 492–498: identity from session JWT / MCP access token / Temporal memo, **never** model JSON (`user_id`/`tenant_id`/`Authorization` untrusted); map `(principal, tenant, tool, args_shape)` → allow / deny / HITL; payments, email send, shell/browser, CRM deletes → `needs_approval`; SSRF deny list; no `execute_sql`; allowlist is capability reduction not authz (§1.2 line 155). Code: `_rbac` + strip model `customer_id` / `idempotency_key` (§5 lines 898–928).
- PII pipeline detect → redact → audit at §4.6 lines 500–504 (DLP at executor **before** persist/log; stable placeholders; WORM placeholder → **hash**, not plaintext). Ingress policy: detect → redact PII before args are logged or cached (§1.2 line 155). Topology Policy box “PII redact” (lines 56–62).
- Immutable telemetry / chain-of-custody at §4.6 lines 506 (WORM: `tool_name`, `call_id`/`tool_use_id`, principal, hashed args, policy decision, downstream status, latency, idempotency key, MCP server hash, breaker state, `correlation_id`; Temporal Event History as second copy; reconstruct charge as policy snapshot + model id + sampled tool_use + hashed args + Stripe status including `Idempotent-Replayed` + human interrupt). Kafka full log as chain-of-custody (§4.1 line 437). Topology `Audit (WORM)` (§1.1 lines 126–135).

**6. Design Case Studies — PASS**
- Exactly two scenarios in §6 (header at line 1200: “Exactly two enterprise designs”).
- **Scenario 1** (§6 lines 1202–1256): payments + CRM, 10k tenants, peak 100 concurrent agents, zero duplicate charges, p95 **< 8 s**, cache-warm **$19.14 / 1k** vs uncached **$33.42** vs **+$0.96** schema-fail; ASCII architecture lines 1208–1241; 3-way matrix (A/B/C) across cost, latency, ops complexity, security, scalability lines 1247–1254; rationale recommends **B** (session-hash keys, disable parallel writes, one retry owner, `extra=forbid`, Temporal Activity per tool) line 1256.
- **Scenario 2** (§6 lines 1257–1308): MCP gateway, 50 servers, thousands of tools, ~55k definition tokens, tool search **>85%** cut, no token passthrough, no page-2 dropouts; ASCII architecture lines 1263–1293; 3-way matrix lines 1299–1306; rationale recommends **B** (gateway OAuth terminate, Redis catalog all pages, `defer_loading` + tool search, host-mapped allowlist) line 1308.

### STATUS: APPROVED
