## Module: 11-production-deployment.md

### Criteria Results
| # | Criterion                        | Verdict |
|---|----------------------------------|---------|
| 1 | ASCII System Topology            | ✅  |
| 2 | Token Cost Economics & SLA        | ✅  |
| 3 | NFR Trade-offs                   | ✅  |
| 4 | Distributed Resilience           | ✅  |
| 5 | Enterprise Security Boundaries   | ✅  |
| 6 | Design Case Studies              | ✅  |

### Deficiencies (if any)
None. All six criteria are met at the cited sections/line ranges below.

**1. ASCII System Topology — PASS**
- Diagram present with box-drawing characters (`┌─┐│└─┘├┤┬┴┼─`) at §1.1 lines 58–145 (not tables-only; plane table at lines 147–156 and clock-owner table at lines 160–165 are accompanying).
- Planes mapped in the diagram: **Control** (lines 66–94: Edge SSO/`cid`/`thread_id`, Policy PII redact BEFORE vendor or checkpoint, RELEASE Cosign+SBOM/pin prompt SHA + snapshot, GATEWAY SKU alias/canary wt/circuit/fallback, BUDGET tenant `max_budget` WITH DB / 80% alert, RELEASE LEDGER image@digest / promptfoo FAILURES>0 / Braintrust `reportRun` / Argo AnalysisRun, Circuit ONE per VENDOR + one MCP cluster, Fallback primary → secondary vendor → determin. NEVER on spend 429, SIGTERM/`request_drain()` super-step / NIM live≠ready / SSE no retry mid-stream), **Data** CI/canary + serving (lines 99–104: fail-closed pin `(dataset,tag)`; online canary fail-open + cov %; simulators — no prod write MCP; Agent Server / LiteLLM/Portkey; optional NIM/vLLM separate image; clock = TTFT / e2e), **Tool proxies** (lines 108–122: Zero-Trust wrap, RFC 8707 `aud`, tenant NEVER from tool args, CI sim NO charge `.execute` vs prod MCP gateway allowlist, NetworkPolicy default-deny, gVisor for untrusted bash), **Persistence** (lines 108–124: CHECKPOINT Postgres/Mongo EncryptedSerializer; TEMPORAL event hist. / WorkflowId / Activity id / InMemorySaver; REDIS pub/sub + queue only; KAFKA/SQS tool bus + DLQ not chat tokens; token vault NOT in checkpoints), **Telemetry** (lines 126–136: Audit WORM cid/tenant/prompt SHA/snapshot/tool hash/rail decision; Metrics `gen_ai.client.token.usage` 100% / TTFT / EPP queue / coverage %; Traces OTel `gen_ai.*` Collector fanout + GUARDRAIL spans; Usage authoritative on terminal event — billed tokens, `$ / good 1k` mix named, remaining budget, spend-cap remaining). Guardrail PEP own-clock sidecar (lines 138–144) is additional, not a substitute for the five required planes.
- Request-flow narrative is §1.2 lines 169–193 (release/control 1–6: PR schema/PII gate → merge pin SHA+snapshot → BuildKit secret + Cosign → canary `setWeight: 10` + AnalysisJob → promote → WORM export; production serving 7–12: ingress token-bound tenant/roles → input PEP cheap→expensive → inner loop MCP-only + 100% `gen_ai.*` → output PEP + deanonymize → checkpoint super-step / Temporal Activity → halt + `$ / good 1k`; SIGTERM/drain 13). Interview talking point line 193.

**2. Token Cost Economics & SLA — PASS**
- Explicit `$ per 1k` with assumptions at §3.1 lines 306–337 (skeleton **W**: one chat turn, **3,000 in + 800 out**, no cache; GPT-5.4 **$2.50 / $15**, Sonnet 4.6 **$3 / $15**, Haiku 4.5 **$1 / $5** from 01, 2026-09-23; `$ / 1k` is **[inferred]** from named SKUs × stated mix). Hop table (lines 314–319): GPT-5.4 **$19.50 / 1k**; Sonnet **$21.00**; Haiku **$7.00**; deterministic **$0**. Mix **M1** 92/6/2 (lines 321–325): **$19.20 / 1k [inferred]** tokens only. Mix **M2** (line 327): **$17.28 / 1k**. Mix **M3** retry storm (line 329): add **$3.98** → **~$23.2 / 1k**. 3-node graph (**05**) multiply ~3 → **~$58.5 / 1k** (line 331). Idle GPU (lines 339–343): aggregator **~$6.88 / H100-hr** → **~$165/day**; break-even vs M1 **~8,600 requests/day**. Why-It-Matters preview lines 19. Speculative generation bills even when rails later block (lines 19, 331, 337).
- Latency tiers with mitigations at §3.3 lines 345–367. ⚠️ No published Agent Server / NeMo / Temporal Activity p50/p95/p99 (lines 7, 19, 308, 346). **[inferred policy]** table (lines 358–364): **p50** user chat = vendor TTFT + cheap rails (keep Llama Guard off default path; warm spaCy); **p95** + output rails, `proxy-buffering: "off"` / Envoy route `timeout` **0**; **p99** idle-gap/decode — raise nginx `proxy-read-timeout` above **60s** (docs examples **3600s**), SSE keepalive **20–30s**, Envoy `stream_idle_timeout` default **5 min**; **p50** NIM Ready = minutes (`startupProbe` 20 min; prePromotion on Ready); **p99** drain = finish super-step (`terminationGracePeriodSeconds` ≥ p99 decode; no retry same stream); **p99** PEP = shed to schema/regex, fail-open only with WORM.
- Throughput and back-pressure at §3.4 lines 368–391: HPA formula every ~15s; do **not** scale on CPU (agents wait on API/GPU); KEDA on `llm_d_epp_flow_control_queue_size` / running requests / inflight tokens / task-queue depth; scale-to-zero cold-start becomes request latency. Shed order (lines 372–378): admit iff serving breaker closed **and** tenant budget remaining; drop online content sample → keep 100% `gen_ai.*` → pause soft rails → keep schema/PII/allowlist → never retry spend-cap 429; if all fallback OPEN, edge 429 `Retry-After`. 429 taxonomy (lines 380–391) separates `slow_down` vs spend/usage/credit vs LiteLLM budget vs gateway overload so KEDA does not treat them as one signal.

**3. NFR Trade-offs — PASS**
- Availability, RPO, RTO, compliance stated with working targets at §3.5 lines 393–403 (availability **99.9% [policy]** on **completed turns** with `finish_reason ∈ {stop, tool_calls}` **and** not vendor spend-429; serving must not depend on Hub / LangSmith UI; **RPO** checkpoints = last durable super-step (`async` small loss window; `sync` smaller; `exit` none mid-graph), Temporal = last completed Activity, goldens/AnalysisRuns = exported JSON, in-flight SSE **gone**; **RTO** serving = roll digest / flip Service (seconds if pulled), GPU Ready ≤ **20 min**, HITL days = Temporal signal not FastAPI worker, CI = replay pinned tag + SHA (minutes); compliance EU PII Presidio sandwich + `hide_inputs` (**10**), PCI no PAN in spans or checkpoints, judges/classifiers = subprocessors, WORM object-lock for stated SOC2/HIPAA retention — **OTel backends are not WORM** unless exported).
- Competing-NFR tensions in the same table: SaaS control-plane outage vs self-host Helm / Hybrid in-VPC vs scale-to-zero task loss; `async` vs disk (**05** TTL) vs Redis-as-RPO=0; fast degraded deterministic vs bit-identical frontier vs cold NIM vs API overflow $; `DeltaChannel` / serde / Mongo vs Postgres consistency; fail-open rails vs missed jailbreak vs indefinite goldens; **cost vs latency** (M1 **[inferred] $19.20 / 1k**, 3-node **~$58.5 / 1k**, idle GPU **~$165/day**, M3 **~$23.2 / 1k**; sync Llama Guard adds p99 **and** $; speculative generation bills blocked prompts; 2× GPU for blue/green); **throughput vs $** (HPA on EPP not CPU; trip cap → shed 402 not fallback storm; idle H100 as TTFT insurance vs 5% util; `minReplicas ≥ 1` vs scale-to-zero).

**4. Distributed Resilience — PASS**
- Durable execution: Temporal / Kafka **equivalent** at §4.1 lines 407–419 (LLM calls and tools = Temporal **Activities**, not Workflow; set `MaximumAttempts` on paid model calls; `non_retryable_error_types` for 400 / auth / spend-cap; Workflow = “thread T, prompt SHA P, snapshot S”; HITL = signal wait; LangGraph checkpoint vs Temporal history vs Store vs outbox; Redis signaling only). Kafka/outbox (line 417): `tool.intents` (idempotency key **before** spend), `tool.receipts` (WORM), `tool.dlq` (poison args, schema 400s), `otel.spans`; compaction on `idempotency_key`; Temporal = saga coordinator, Kafka = firehose; do not DLQ chat tokens. Resume semantics also at §2.6 lines 267–275 (LangGraph durability `exit`/`async`/`sync`; node body at-least-once; Temporal plugin `InMemorySaver`; idempotency key = **Workflow Run ID + Activity ID**; saga compensations reversed). Encoded in `CheckpointStore` / `ToolBus` (§5 lines 761–804, 1070–1071).
- Failure taxonomy at §4.2 lines 421–443: **transient** (408/`slow_down`/503/529, TLS reset, NIM NotReady, OTLP 429 — full jitter; honor `Retry-After` iff \(0<t\le 60\); same idempotency key) / **permanent** (400 schema, `ContextWindowExceeded`, 401/403, spend/usage/credit 429, LiteLLM budget 400, jailbreak, `aud` miss — **no** retry) / **poison pill (deploy)** Hub `latest` / 5xx-only canary / unsigned `:latest` / arm64 into amd64 / **poison pill (checkpoint)** renamed node / `DeltaChannel` / missing `LANGGRAPH_AES_KEY` / **poison pill (spend)** treat spend 429 as `slow_down` / no-DB fail-open / **poison pill (rails)** silent `fix` / fail-open no log / **poison pill (secrets)** ARG tokens / tokens in `state`. Idempotency: key on every mutating tool (lines 28, 273, 294, 417, 443). Encoded in `TransientError` / `PermanentError` / `SpendCapError` / `retry_with_jitter` (§5 lines 573–666).
- Circuit breaker closed → open → half-open at §4.2 lines 445–459 (one breaker per **vendor** plus one per **MCP cluster**, **distinct from** the judge breaker (**10**); do not open solely on 429-with-Retry-After; do not open serving because the judge 429d; half-open probe is a **cheap** 1-token classify / `/ok`, not a 12-minute decode; if all open, LiteLLM health-filter and Portkey breaker **bypass** — shed at the edge), including ASCII state diagram lines 447–459. Encoded in `BreakerStateMachine` (§5 lines 597–637, 1009–1024).
- Fallback chain: primary (pinned snapshot) → secondary **other-vendor** (different failure domain) → **deterministic** template / HITL queue; **never** invent tokens from a heuristic and bill them; `PermanentError` on spend / schema / RBAC **does not** failover to a second paid SKU (lines 259–265, 461). Encoded in `ServingRuntime.complete` (§5 lines 826–872, 978–995).

**5. Enterprise Security Boundaries — PASS**
- Zero-Trust MCP at §4.3 lines 463–469: agent → MCP **only** through a gateway PEP — OAuth 2.1, PRM RFC 9728, resource indicators RFC 8707; **no shared PAT**; tool allowlists; RFC 8707 token minted for the gateway resource **must not** work on raw GitHub MCP (**08**; confused deputy). NetworkPolicy default deny; **no** worker egress to the public internet except the MCP gateway and pinned vendor CIDRs; gVisor/Firecracker for untrusted `bash`. Topology Zero-Trust box (lines 108–116); request-flow step 9 (line 184). Encoded in `mint_mcp_token` / `GATEWAY_AUD` (§5 lines 905–910, 1026–1037).
- Tool-level RBAC / least privilege at §4.3 lines 467: allowlists compiled into the worker image (or signed ConfigMap); parameter allowlists (URL prefixes, max refund, forbidden SQL verbs); tenant identity from the **JWT**, not prompt field or tool arg; Temporal resume **re-RBAC** before any write tool; separate IdP clients for CI vs prod — CI eval bots must not inherit `refund.execute` (**10**). Encoded in `ALLOWED_TOOLS` / `WRITE_TOOLS` / `GuardrailPep.check_tool` / `mint_mcp_token` deny of `payments.charge` (§5 lines 520–521, 754–758, 905–910, 961–971, 1033–1037).
- PII pipeline detect → redact → audit at §4.4 lines 471–473 (Presidio on input **and** logs; pipeline **detect → redact → audit** at ingress, before vendor, before checkpoint, before dataset promote; sandwich: session map, not raw email in the prompt). Topology Policy “PII redact BEFORE vendor or checkpoint” (lines 71–74); request-flow steps 8 and 10 (lines 183, 185); defense-in-depth cheap→expensive (§2.3 lines 239–243). Encoded in `PiiSandwich` / `GuardrailPep.check_input` / `check_output` (§5 lines 699–752, 944–949).
- Immutable telemetry / chain-of-custody at §4.4 lines 475–479 (WORM append-only row: `timestamp, correlation_id, tenant, user_hash, model_snapshot, prompt_commit, tool_name, args_hash, gen_ai.usage.*, billed_usd, guardrail_decision, rail_name, finish_reason, thread_id, checkpoint_id, spend_remaining, hide_inputs=true`; S3 Object Lock / Azure immutable storage; **OTel backends are not WORM** unless exported; MCP log `tools/call` name + hash, not secrets). Kafka full log = chain-of-custody (§4.1 line 417). Topology `Audit (WORM)` (§1.1 lines 128–135). Encoded in `JsonLogFormatter` / PEP `worm` / runtime `worm` (§5 lines 529–541, 728–732, 897–901, 1044–1045).

**6. Design Case Studies — PASS**
- Exactly two scenarios in §6 (header at line 1079: “Exactly two enterprise designs”).
- **Scenario 1** (§6 lines 1081–1136): API-only Kubernetes SaaS agent, multi-tenant chat+tools, no GPU, EU PII, **99.9%** completed-turn availability, mix M1 **[inferred] $19.20 / 1k**, 3-node ≈ **$58 / 1k**, idle GPU **$0**; ASCII architecture lines 1087–1122; 3-way matrix (A/B/C) across cost, latency, ops complexity, security, scalability lines 1126–1134; rationale recommends **B** (distroless digest + Cosign, Helm Agent Server min≥2, LiteLLM pin + DB budget, CPU PEP sandwich, Argo 10% + 10’s gate, Postgres async + EncryptedSerializer, MCP gateway only) line 1136.
- **Scenario 2** (§6 lines 1138–1195): hybrid GPU + Temporal resume, NIM bulk + frontier overflow, HITL days, charging tools; GPU **~$6.88/H100-hr**; 10% API overflow on 100k NIM-local turns **≈ $192**; ASCII architecture lines 1144–1178; 3-way matrix lines 1185–1193; rationale recommends **B** (two images; Temporal Activities + InMemorySaver; KEDA on EPP; saga keys; NeMo IGW on both; API overflow on NIM 503; blue/green prePromotion) line 1195.

### STATUS: APPROVED
