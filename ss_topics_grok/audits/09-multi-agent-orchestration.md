## Module: 09-multi-agent-orchestration.md

### Criteria Results
| # | Criterion                        | Verdict |
|---|----------------------------------|---------|
| 1 | ASCII System Topology            | ✅  |
| 2 | Token Cost Economics & SLA        | ✅  |
| 3 | NFR Trade-offs                   | ✅  |
| 4 | Distributed Resilience           | ✅  |
| 5 | Enterprise Security Boundaries   | ✅  |
| 6 | Design Case Studies              | ✅  |

### Deficiencies (if any)
None. All six criteria are met at the cited sections/line ranges below.

**1. ASCII System Topology — PASS**
- Diagram present with box-drawing characters (`┌─┐│└─┘├┤┬┴┼─`) at §1.1 lines 49–137 (not tables-only; plane table at lines 140–149 and next-token-owner table at lines 151–159 are accompanying).
- Planes mapped in the diagram: **Control** (lines 58–86: Edge SSO/`cid`/`thread_id`, Policy PII redact before any hop copies the transcript, SUPERVISOR/ROUTER next-agent + hop/$/fan-out caps, JOIN/VOTE τ accept|extra round|ESCALATE never avg, HITL GATE timeout-DENY, ORCHESTRATION LEDGER typed Handoff + downscope in `on_handoff` + four edges SLA/confidence/cost/security, Circuit ONE per specialist/provider not one global, Fallback primary specialist → peer → degraded JSON, SIGTERM/drain park HITL / do not cut in-flight research), **Data** specialists + HITL/A2A (lines 91–104: FAQ/billing/research/judge inner ReAct; Temporal Signal / Kafka `hitl.approvals`; A2A Task SUBMITTED→WORKING→COMPLETED|FAILED|CANCELED|REJECTED|INPUT_REQUIRED|AUTH_REQUIRED; terminal immutable; sibling tasks share `contextId`), **Tool proxies** (lines 108–122: Zero-Trust wrap, RFC 8707 `aud`, tenant NEVER from tool args, FAQ MCP read-only vs payments write+HITL, researcher/judge artifact read NEVER mutate Stripe, downscope `jti` at handoff), **Persistence** (lines 108–123: THREAD/`RunState`/`active_agent`/hops/`$ spent`, HITL ROW `sla_s`/decision/timeout-deny, ARTIFACTS fs refs + A2A ids, VOTE LOG vector/τ/dropped hash, token vault not in messages), **Telemetry** (lines 125–137: WORM cid/from/to/mechanism/`jti`/`tools_enabled`/τ/`vote_vector`/`human_gate`/artifact ids; metrics hops/$/p50/p95/p99 [policy]/HITL wait/ping-pong/breaker per agent; traces supervisor→specialist→join + A2A `taskId` with PII stripped; usage calls×SKU/search SKU/`total_cost_usd`/`remaining_usd`).
- Request-flow narrative is §1.2 lines 161–181 (interactive path 1–10: ingress token-bound ids → DLP before any specialist → supervisor tick runtime-enforced caps → typed Handoff + downscope + raise on authz fail → specialist inner loop → join at τ or escalate → HITL Temporal/Kafka park → four escalation edges → A2A `SendMessage` with immutable terminal tasks → halt+WORM reconstruct a refund; durable HITL dual-path 11–14: persist `RunState` before process death → SLA timer outside the graph timeout→manager→auto-reject → resume outermost graph → overflow sheds low-priority, never auto-approves irreversible class).

**2. Token Cost Economics & SLA — PASS**
- Explicit `$ per 1k` with assumptions at §3.1 lines 331–366 (stated Loop S = L1 ticket, supervisor + 2 specialists; reference call **2,000 input + 400 output**; Claude Sonnet 5 **$2 / $10** per MTok → **$0.008 / call**; model tokens only unless a row names a SKU; cache hit **$0.20 / MTok**). Table (lines 341–348): subagents 4 calls **$32 / 1k**; sticky handoffs 3 calls **$24 / 1k**; router+parallel **$40 / 1k**; GPT-5.4 subagents **$44 / 1k**; repeat turn 2 handoffs **$16 extra** vs subagents **$32 extra**. Coordination tax **+$8 / 1k / turn** (line 350). Loop R (line 352): chat **$0.009** → 4× **$36 / 1k** → 15× **$135 / 1k**; 30% Opus 5 mix **~$240 / 1k**; web-search **$10 / 1K**, 3×8 = **$0.24 / task**. Loop D 50×10 (line 354): **$4,000 / 1k**. Debate extras (lines 358–364): majority N=3 **+$14 / 1k**; Du 3×2 **+$56 / 1k**; SC k=5 **+$28**; SC k=40 **+$273**. Prompt-cache assumption (line 366): supervisor + worker playbooks cached; hierarchical shared prompts best cache shape.
- Latency tiers with mitigations at §3.2 lines 383–392. **[inferred policy]** (not vendor SLOs; gap noted lines 331, 335, 370): **p50** Loop S **2–4 s** parallel `Send` / **3–6 s** serial (`parallel_tool_calls`/`Send` when independent; cache supervisor prefix; `last_message` + filesystem refs); **p95** Loop S **5–8 s** parallel / **8–12 s** serial (cap tool I/O; disable extra debate on L1; sticky swarm turn 2 skips router); **p99** Loop S **fail closed at 9–15 s** with hop cap (hop cap **3** support / effort cap research; per-specialist breaker; degraded JSON; unbounded `Send`/ping-pong has **no** p99). Worked envelope (line 381): sequential **~9.0 s** vs parallel **~5.5 s** vs sticky turn 2 **3.5 s**. HITL decision p95 is a queue SLO (15 min L1 / 4 h L2), not added to model p99 (line 391).
- Throughput and back-pressure at §3.3 lines 394–412: in-flight completions supervisor+2 specialists = **3** during a wave (multiplies provider RPM/ITPM); `Send` width admission-control **W** in code (hard cap <<50); OpenAI `max_turns` default **10**; Magentic `max_stalls=3`/`max_turns=20`; debate 3×2 = **9** utterances. Admit the ticket iff supervisor breaker ∈ {closed, half-open} **or** cheap-router/degraded; shed skip debate on L1 → cap `Send` → forbid new handoffs at 80% `$` → HITL-shed low-priority → deterministic `status: "degraded"`; never 50-retry a poison specialist; never auto-approve irreversible class; size in-flight completions not just user QPS.

**3. NFR Trade-offs — PASS**
- Availability, RPO, RTO, compliance stated with working targets at §3.4 lines 414–425 (availability **99.9% control plane [policy]**; per-specialist breaker; Cursor-style isolation; A2A `FAILED` ↛ kill `contextId`; **RPO** interactive=last checkpointed super-step / persisted `RunState`, HITL=last queue row, A2A=last Task status with terminal immutable, artifacts=object-store refs; `InMemorySaver`/process-held OpenAI run: crash loses the approval; **RTO** stateless resume from checkpoint/`RunState`/`taskId`, interactive hop timeout **2–8 s [policy]** then breaker, HITL RTO = queue SLA minutes–hours, rainbow dual-run + pin prompt version on the thread; compliance AISVS C9 **9.1.2**/kill-switch **9.1.3/9.6.3**/approval **9.2**/timeout-deny **9.6.2**/per-agent tools **9.5.1**/downscoped token **9.5.2**/policy engine not the model **9.5.3**/delegation **9.5.5**/crypto identity **9.4.1**, OWASP ASI03/07/08/09, EU AI Act Art. 14, WORM of handoff/vote/HITL, MCP MUST NOT passthrough).
- Competing-NFR tensions in the same table: supervisor SPOF on serial topology vs cheap Haiku router + Opus-on-complexity; failover **busts** prefix cache vs availability; fast degraded JSON vs bit-identical specialist result vs rainbow-unsafe Pregel schema cutover; `active_agent` LastValue vs parallel handoff race; hosted lead vs VPC tools / nested handoff history re-embeds PII / A2A artifacts crossing orgs; cost vs latency (Loop S **$24–32 / 1k**, Loop R **$135–240 / 1k**, debate **+$56 / 1k**, Loop D **$4,000 / 1k**; parallel `Send` **~9.0 s → ~5.5 s** and multiplies RPM; sticky handoff saves **$8 / 1k / turn** and loses centralized join); isolation vs tokens (`input_filter` too aggressive vs must log hash of dropped items).

**4. Distributed Resilience — PASS**
- Durable execution: Temporal / Kafka **equivalent** at §4.1 lines 430–453 (Start Workflow = first supervisor tick writes `(thread_id, active_agent, hops=0, remaining_usd, policy_version)` **before** any specialist tool; **idempotency key** for mutating tools minted by the **session**, never the model; Activity = specialist inner loop / MCP `tools/call`; HITL Signal = persist checkpoint/`RunState` → `wait_condition(..., timeout=)` zero compute → escalate → auto-reject; A2A as saga with immutable terminal + refinement `taskId`; **DLQ** ping-pong / repeating 500s / HITL past second timeout / A2A `FAILED` with no refinement). Kafka/outbox mapping: `orch.intents`, `orch.handoffs`, `orch.votes`, `hitl.approvals`, `orch.dlq`, `orch.artifacts`; compaction on `thread_id`; full log = chain-of-custody (line 440). Replay vs resume: time-travel re-fires interrupts; not safe to retry `issue_refund`/`send_email` — map duplicates to original receipt (line 442). Durable-after table (lines 444–453).
- Failure taxonomy at §4.2 lines 470–485: **transient** (408/429/5xx/529, TLS reset, specialist timeout — full jitter, same idempotency key, do not retry RBAC deny) / **permanent** (400 schema, 401/403, hop/`$` cap, τ_low, RBAC deny, A2A `REJECTED`, `aud` miss) / **poison pill (ping-pong)** / **poison pill (fan-out)** / **poison pill (vote)** / **poison pill (HITL)** / confused deputy / context loss / rainbow-unsafe deploy. Idempotency: session-minted key (line 434); Stripe idempotency from session in Scenario 1 (line 1180); durable-after maps duplicates (line 442).
- Circuit breaker closed → open → half-open at §4.2 lines 488–502 (one breaker per specialist **plus** per provider **plus** per A2A callee; do not open solely on 429-with-Retry-After; do not open billing because web-search 429d; half-open probe is a **cheap read** / `kb_search` or canary `tasks/get`, not `issue_refund`), including ASCII state diagram lines 490–502. Encoded in `BreakerStateMachine` (§5 lines 660–700).
- Fallback chain: primary specialist (audience-bound, downscoped) → secondary **equivalent** specialist (same IR, **new** `jti`) → **deterministic** schema-valid decline (`status: "degraded"`, no refund, no email); supervisor-class open → cheap router or degraded JSON, **not** a second payments processor; `PermanentError` on hop/`$`/τ/RBAC **does not** failover to the supervisor’s union toolset or token passthrough (line 504).

**5. Enterprise Security Boundaries — PASS**
- Zero-Trust MCP at §4.3 lines 506–526: never trust specialist arguments for tenant/identity; two confused-deputy layers (OAuth proxy **08** vs agent deputy: supervisor executes worker calls with supervisor creds); downscope at handoff — `on_handoff` mints token whose audience is worker MCP servers and scopes match the brief; RFC 8707 `aud` on **every** MCP hop; MCP **MUST NOT** passthrough; AISVS **9.5.1–9.5.5** / **9.4.1**; short-lived per-agent per-session credentials. Topology Zero-Trust box (lines 108–109). Encoded in `mint_downscoped` / no passthrough (§5 lines 800–810).
- Tool-level RBAC / least privilege at §4.3 lines 512–526: principal table (router/lead must not hold production write tools; judge reads artifacts only; A2A callee cannot reach VPC except via published artifacts); **RBAC tuple** `(principal, tenant, from_agent, to_agent, tools_enabled, jti, policy_version)`; resume/`Command(resume=)` re-RBAC; isolation ladder OAuth scope → gateway `Mcp-Name` allowlist (**08**) → per-agent orchestrator allowlist → separate MCP servers → separate conversations. Encoded in `AGENT_TOOLS` / `TRANSITIONS` / `mint_downscoped` (§5 lines 581–597, 800–810).
- PII pipeline detect → redact → audit at §4.4 lines 528–532 (ingress **and** before each handoff splice **and** before trace export; DLP after specialist result before next model turn; PCI: PAN never in `messages`; judge sees artifact ids + redacted claims). Topology Policy “PII redact BEFORE any hop copies the transcript” (lines 61–66); request-flow step 2 (line 166). Encoded in `redact_pii` (§5 lines 730–731).
- Immutable telemetry / chain-of-custody at §4.4 lines 534–538 (WORM append-only: timestamp, trace_id, from/to, mechanism, `input_type`, `principal_id`, `token_jti`, `tools_enabled`, `policy_version`, `human_gate`, artifact ids, vote_vector, τ, dropped_hash, remaining_usd, hops; reconstruct a refund as policy snapshot + typed Handoff + hashed omitted history + downscoped jti + HITL decision + Stripe status — request-flow step 10 lines 174; Temporal Event History / Kafka full log **is** the audit — line 440; provider traces are not a SIEM). Topology `Audit (WORM)` (§1.1 lines 128–136).

**6. Design Case Studies — PASS**
- Exactly two scenarios in §6 (header at line 1125: “Exactly two enterprise designs”).
- **Scenario 1** (§6 lines 1127–1182): L1/L2 B2B SaaS support, 1k tickets/day, SOC2, sticky owner, `issue_refund` HITL 15 min L1 / 4 h L2 timeout-deny, Loop S handoff **[inferred] ~$24 / 1k** vs subagents **~$32 / 1k** vs GPT-5.4 **~$44 / 1k**, hop cap **3**; ASCII architecture lines 1133–1168; 3-way matrix (A/B/C) across cost, latency, ops complexity, security, scalability lines 1174–1180; rationale recommends **B** (sticky triage handoff, typed `EscalationData`, hop cap 3, Temporal HITL timeout-deny, per-agent downscope) line 1182.
- **Scenario 2** (§6 lines 1184–1239): analyst parallel research + judge, Loop R **[inferred] $135 / 1k** Sonnet 15× or **~$240 / 1k** 30% Opus, web search **$0.24 / task**, hard W cap, never majority-join summaries; ASCII architecture lines 1190–1225; 3-way matrix lines 1231–1237; rationale recommends **B** (orchestrator-worker, isolated windows, filesystem refs, hard W cap, one CitationAgent/judge, `$` freeze) line 1239.

### STATUS: APPROVED
