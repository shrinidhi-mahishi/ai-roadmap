## Module: 07-agentic-rag.md

### Criteria Results
| # | Criterion                        | Verdict |
|---|----------------------------------|---------|
| 1 | ASCII System Topology            | ✅  |
| 2 | Token Cost Economics & SLA        | ✅  |
| 3 | NFR Trade-offs                   | ✅  |
| 4 | Distributed Resilience           | ✅  |
| 5 | Enterprise Security Boundaries   | ✅  |
| 6 | Design Case Studies              | ✅  |

### Deficiencies (if any)
None. All six criteria are met at the cited sections/line ranges below.

**1. ASCII System Topology — PASS**
- Diagram present with box-drawing characters (`┌─┐│└─┘├┤┬┴┼─`) at §1.1 lines 42–126 (not tables-only; plane table at lines 128–138 and ingest-vs-query table at lines 140–145 are accompanying).
- Planes mapped in the diagram: **Control** (lines 50–80: edge auth/SSO/`tenant` from token/MCP audience, policy PII redact before embed/Vec2Text, Adaptive router chitchat|factoid|multi-hop|global, loop retrieve→grade→rewrite→generate with hop cap=3 + wall-clock, alias pin query=N ingest=N+1, two-plane orchestrator query PEP→hybrid→RRF/RSF/α→rerank tool vs ingest watermark→DLP→ACL→chunk→alias flip, circuit search≠rerank≠generate≠ingest, fallback hybrid→BM25-only→refuse, SIGTERM drain finish hop / do not half-flip alias), **Data** query/generate + ingest (lines 85–94: tokenizer → prefill → decode, prompt = instr + top_n, citation IDs ⊆ \(R\); Temporal/Kafka parse/chunker_version/sha256, DLP before embed and contextualize, ACL copy child and parent, dense+sparse same text, parent docstore version = vectorstore), **Tool proxies** (lines 98–112: Zero-Trust wrap, RFC 8707 audience, tenant never from tool args, `retrieve_kb/_hr` no omnibus `search()`, `rerank_api` own RPM/timeout/cache, graph_local vs graph_global, CRAG web allowlist, observation ≠ corpus write), **Persistence** (lines 99–113: five indexes — dense ANN+ACL bitmap, sparse BM25/SPLADE, parent docstore+alias, rerank cache `(q, doc, model)`, optional graph), **Telemetry** (lines 115–125: WORM cid/tenant/\(R\) ids/spans/`index_build`/cite∉R rate, retrieve p50/p95/p99, hop depth, nDCG, alias lag, breaker, traces gateway→PEP→ANN∥BM25→RRF→rerank→grade with PII stripped, usage embed tok / rerank units / generate tok / hops / `total_cost_usd` on terminal event).
- Request-flow narrative is §1.2 lines 149–172 (query path 1–10: ingress token-bound ids → PII+RBAC+search breaker → Adaptive router → PEP every arm → hybrid retrieve → rerank tool → grade → rewrite/generate with checkpointed hop cap → optional parent expand with re-ACL → halt+WORM; ingest path 11–16: watermark+sha256 → parse/DLP/ACL → chunk → upsert N+1 → shadow nDCG+citation-NLI → alias flip → canary CODE-CANARY-9).

**2. Token Cost Economics & SLA — PASS**
- Explicit `$ per 1k` with assumptions at §3.1 lines 337–360 (reference query = 1k questions, no retries; query embed 50 tok; retrieve 80 fused; rerank 80; keep 8 × 500 tok = 4k context; generate 4k in / 400 out; dense embedder OpenAI `text-embedding-3-small` **$0.02/1M**; Voyage `rerank-2.5` 44k tok/query × $0.05/1M = **$0.0022/query**; generator Claude Sonnet 5 **$2 / $10** per MTok). Formula \(C_{\mathrm{q}}\) at lines 341–343. Table: query embed **$0.001 / 1k**; Voyage rerank **$2.20 / 1k**; generate **$12.00 / 1k**; **subtotal ≈ $14.2 / 1k** (lines 345–350). Mini-tier flip → **~$3.0 / 1k** and rerank dominates (line 352). Pinecone RU overlay **$0.016 / 1k** (1 GB ns) vs **$1.60 / 1k** (100 GB) (line 354). Cache assumption: pin system + tool schemas, not the chunks (line 360). Hop overlay at §3.2 lines 362–375: 20% rewrite-once **~$3–4 / 1k**; uncapped CRAG+web 3 hops **tens of $ / 1k**.
- Latency tiers with mitigations at §3.3 lines 377–397. **[inferred] policy targets** (not vendor SLOs; gap noted lines 335, 434): **p50** retrieve+rerank **150–400 ms** (hot rerank cache with ACL in key; skip retrieve on Adaptive-A); **p95** retrieve+rerank **400 ms–1.5 s** (cap fused \(N\); Voyage lite + ≤200k tok; drop to fused top-8 on rerank timeout); **p99** retrieve+rerank **fail closed at 1–3 s** (search breaker; BM25-only; then `retrieval_degraded`); **p50** e2e **0.8–2 s**; **p95** e2e **2–6 s** (hop cap=3; grade on Haiku; no HyDE on SKUs); **p99** e2e **8–15 s** with hop cap=3 (wall-clock fuse; `insufficient_evidence`; unbounded rewrite has **no** p99).
- Throughput and back-pressure at §3.4 lines 401–420: Cohere Rerank **trial 10 / production 1,000 RPM**; 1k user QPS × 3 hops = **3k retrieve RPM** — size for the loop; Voyage ≤1,000 docs / ≤600k tok; Pinecone 1 RU/GB of **that** namespace; hop cap **3** + wall-clock. Admit query iff search breaker ∈ {closed, half-open} **or** BM25-only/refuse; shed skip rerank → BM25-only → `retrieval_degraded`; ingest queues, never blocks query p99; hedge retrieve budgets 2× RU.

**3. NFR Trade-offs — PASS**
- Availability, RPO, RTO, compliance stated with working targets at §3.5 lines 422–432 (availability **99.9% gateway**; search fail-**closed** on policy answers (BM25 then refuse); generate 503/529 → secondary model **with the same \(R\)**; Pinecone Enterprise **99.95%** uptime SLA vs Starter/Standard **none**; **RPO** query=last flipped alias (N still readable), ingest=last acked watermark, rerank cache minutes best-effort, checkpointer `retry_count`; **RTO** retrieve timeout **200–500 ms [policy]** then BM25 then refuse, alias rollback minutes, full re-embed hours–days is **not** interactive RTO; compliance ACL predicate, per-tenant ns or BYOC, DLP before embed+contextualize, WORM of \(R\)+spans+`index_build_id`, 21 CFR 11 / legal-hold = WORM + prompt replay, Bedrock guardrails do **not** cover retrieved source text).
- Competing-NFR tensions in the same table: failover **busts** prefix cache vs availability / never failover a 400 schema; dual-write 2× WU vs serving N+1 early; fast BM25 vs bit-identical hybrid / QUORUM vs retrieve latency (Weaviate `ONE` can cite a replica missing the latest delete); eventual ANN vs litigation-hold “this version, this minute”; Contextual Retrieval **spreads PII** into every chunk / vectors = DLP class of source text; cost vs latency (**~$14.2 / 1k** vs mini **~$3** vs 20% rewrite **+$3–4** vs uncapped **tens of $**; Adaptive-A skip vs always-C **3.33×** time); retriever/rerank cache must include ACL + `index_version` vs hit rate / alias flip **must** bust retrieve caches.

**4. Distributed Resilience — PASS**
- Durable execution: Temporal / Kafka **equivalent** at §4.1 lines 440–477 (ingest Workflow = parse → DLP → ACL stamp → chunk → embed → sparse → parent upsert with idempotency `chunk_id = hash(doc_id, chunker_version, text)` plus `embed_model + dim`; watermark topic; alias flip as single control-plane transaction after shadow eval; parent docstore swapped under one `index_version`; **DLQ** poison blobs; query checkpointer `retry_count` / `original_question` / `search_query` / \(R\)). Kafka/outbox mapping: `rag.intents`, `rag.chunks`, `rag.upserts`, `rag.alias_flip`, `rag.dlq`, `rag.erasure`; compaction on `doc_id`; full log = chain-of-custody (line 474). Replay vs resume: ingest Activity retry re-runs embed — must be idempotent on `chunk_id` (line 476). Searchable-after table (lines 464–472).
- Failure taxonomy at §4.2 lines 480–498: **transient** / **permanent** / **poison pill (docs)** / **poison pill (ops)** / citation theater / parent-child mismatch / rewrite loops / stale indexes / embedding drift / filter-ANN / over-retrieval / Contextual PII spread / Cohere unit inflation / CRAG open web / rerank RPM. Idempotency: key on ingest (line 444); Activity retry must be idempotent (line 476); ingest `upsert` bind (line 546). Poison recovery (line 500): quarantine by source+sha256; no auto-promote web/tool into corpus; hard-delete + HNSW compaction; alias rollback to N; replay canaries.
- Circuit breaker closed → open → half-open at §4.2 lines 502–516 (one breaker per search/ANN, rerank, generate, ingest embedder, MCP retrieve server; do not open solely on 429-with-Retry-After; half-open probe is a **cheap read** / canary `doc_id` fetch, not a 150-doc rerank; search fail toward BM25-only then refuse), including ASCII state diagram lines 504–516. Encoded in `BreakerStateMachine` (§5 lines 651–696).
- Fallback chain: hybrid (dense∥BM25 + RRF + rerank) → (search breaker / timeout) → **BM25-only** (same ACL) → (both fail or empty \(R\) on policy corpus) → **`retrieval_degraded` refusal**; last-good retrieve cache keyed by ACL sits in front of BM25-only; `PermanentError` on RBAC / cite-∉-\(R\) **does not** failover to shared index or parametric knowledge; rerank timeout → fused top-8; generate 529 → secondary model, **same \(R\)** (line 518). Encoded in fallback / `deterministic_degraded` (§5 lines 1249–1251, 1205).

**5. Enterprise Security Boundaries — PASS**
- Zero-Trust MCP at §4.3 lines 520–534: retrieve `tools/call` is a data-exfil API; tenant/ACL from verified token / `RunContext`, never from tool args; ABAC before search; separate MCP servers `retrieve_public_kb` vs `retrieve_hr` vs `sql_customer` vs `rerank_api`; no omnibus `search(query, collection)`; `/mcp` stateless per request; hop cap in checkpointer; CRAG web allowlist + strip identifiers; re-validate at execution; dual-LLM quarantine of untrusted chunks. Remote servers OAuth 2.1 resource servers; RFC 9728; **RFC 8707** `resource` indicator; PKCE; MUST validate audience; MUST NOT token-passthrough. Topology Zero-Trust box (lines 98–111). Encoded in `ZeroTrustRetrieveProxy` (§5 lines 1208–1229).
- Tool-level RBAC / least privilege at §4.4 lines 536–550: map `retrieve_public_kb` vs `retrieve_hr`/`retrieve_legal` vs `rerank_api` vs `graph_local` vs `graph_global` vs CRAG `web_search` vs ingest `upsert`; `(principal, tenant, tool, collection)` → allow / deny (request-flow line 154); namespace from **token**; resume values re-RBAC before a new retrieve. Isolation ladder: metadata filter → ns-per-tenant → BYOC. Encoded in HR deny / confused-deputy reject (§5 lines 1208–1229).
- PII pipeline detect → redact → audit at §4.5 line 556 (ingress **and** before embed **and** before contextualize **and** before trace; DLP after retrieve before prompt; PCI: no PAN in chunks). Topology Policy “PII redact BEFORE embed” (lines 54–57); request-flow step 2 (line 154). Encoded in `redact_pii` (§5 lines 730–732, 1206).
- Immutable telemetry / chain-of-custody at §4.5 line 558 (WORM: `correlation_id`, tenant, hashed user, \(R\) chunk_ids+sha256, rerank scores + model id, `char_span`s, `index_build_id` / alias, hop depth, `original_question` vs `search_query`, breaker state, tool name, CRAG outbound URL; reconstruct as policy snapshot + \(R\) + hashed chunk bodies + generate; provider traces are not a SIEM). Topology `Audit (WORM)` (§1.1 lines 117–124). Kafka full log as chain-of-custody (§4.1 line 474).

**6. Design Case Studies — PASS**
- Exactly two scenarios in §6 (header at line 1267: “Exactly two enterprise designs”).
- **Scenario 1** (§6 lines 1269–1320): multi-tenant support KB, Sonnet 5 hybrid+rerank **~$14.2 / 1k**, 20% rewrite **+$3–4 / 1k**, Adaptive-A skip on 60% chitchat, citation ID \(\notin R\) target **0**, retrieve timeout **200–500 ms [policy]** then BM25 then refuse; ASCII architecture lines 1275–1306; 3-way matrix (A/B/C) across cost, latency, ops complexity, security, scalability lines 1310–1318; rationale recommends **B** (ns-per-tenant, hybrid RRF/low-α, rerank 80→8, Adaptive front door, parent-child ACL copy, hop cap 3, BM25 then refuse) line 1320.
- **Scenario 2** (§6 lines 1322–1369): legal/research agentic RAG, 21 CFR 11 / litigation hold, no open web, same **$14.2** baseline plus hop cost at cap **2–3**, HippoRAG PPR **10–20×** cheaper than iterative retrieve in-paper; ASCII architecture lines 1328–1355; 3-way matrix lines 1359–1367; rationale recommends **B** (hybrid + BAA rerank 150→20, parent=section, ID-constrained cites + quote spans, CRAG internal-only, hop cap 2–3, WORM replay) line 1369.

### STATUS: APPROVED
