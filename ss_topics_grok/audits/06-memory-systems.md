## Module: 06-memory-systems.md

### Criteria Results
| # | Criterion                        | Verdict |
|---|----------------------------------|---------|
| 1 | ASCII System Topology            | ✅  |
| 2 | Token Cost Economics & SLA        | ✅  |
| 3 | NFR Trade-offs                   | ✅  |
| 4 | Distributed Resilience           | ✅  |
| 5 | Enterprise Security Boundaries   | ✅  |
| 6 | Design Case Studies              | ✅  |

### Deficiencies (if any)
None. All six criteria are met at the cited sections/line ranges below.

**1. ASCII System Topology — PASS**
- Diagram present with box-drawing characters (`┌─┐│└─┘├┤┬┴┼─`) at §1.1 lines 43–123 (not tables-only; plane table at lines 127–135 and write/read table at lines 141–146 are accompanying).
- Planes mapped in the diagram: **Control** (lines 50–77: edge auth/SSO/`tenant_id` from token/MCP audience, policy PII redact before embed/Vec2Text, read vs write split/constructor budget/ACL→ANN, write admission origin tag/idempotent queue not TTFT, compact persist-then-drop at 70%, four-plane orchestrator STM/LTM/episodic/compressor/sleep-time/watermark, circuit retrieve≠write≠embedder, fallback STM+cached profile, SIGTERM drain ack extract), **Data** generation + extractor (lines 82–90: tokenizer → prefill → decode, prompt = card + STM + packed LTM; Temporal/Kafka pair-wise/ADD-only extract, Graphiti NER+reflection, origin user|sleep_time not web/tool, idempotency `(thread, ckpt, memory_id)`), **Tool proxies** (lines 94–109: Zero-Trust wrap, RFC 8707 audience, `user_id` never from tool args, search/read pre-filter vs insert/write ACL, sleep-time allow-list, `/memories` path-traversal deny, observation ≠ belief), **Persistence** (lines 95–110: four stores — STM checkpointer/Letta last-k, LTM Mem0/Zep/Store/Pinecone ns, episodic Graphiti/transcripts/checkpoints, compressor trim/offload/compact log **not** a store), **Telemetry** (lines 112–122: WORM cid/tenant/hashed user/`memory_id`/origin/k/Art.17 ids, retrieve p50/p95, extract lag, watermark, compact loss, `memory_miss`, traces gateway→ACL→ANN→pack→LLM with PII stripped, usage constructor tok / extract tok / embed / SKU retrievals / `total_cost_usd` on terminal event).
- Request-flow narrative is §1.2 lines 148–172 (read path 1–11: ingress token-bound ids → PII+RBAC → retrieve breaker fail-open → STM load → LTM ACL-before-ANN → episodic overlay → pack constructor budget → optional compress after persist → generate → ack extract → WORM; write path 12–16: origin admit → Temporal/Kafka extract → idempotent upsert → watermark → compact persist-then-drop).

**2. Token Cost Economics & SLA — PASS**
- Explicit `$ per 1k` with assumptions at §3.1 lines 314–348 (reference turn = 1 retrieve + inject ~2–7k memory tokens + async extract of a user+assistant pair; generator Claude Sonnet 5 **$2 / $10** per MTok or GPT-5.4 **$2.50 / $15**; 500 out). Formula \(C_{\mathrm{turn}} = (N_{\mathrm{in}}\,P_{\mathrm{in}} + 500\,P_{\mathrm{out}})/10^{6}\) (lines 322–324). Tables: Mem0-like 2.8k in **~$11 / 1k**; Mem0 v3 ~8k **~$21 / 1k**; Zep 2.6k **~$10 / 1k**; stuff LOCOMO 26k **~$59 / 1k**; LME_S 115k **~$237 / 1k** (lines 326–332). Memory SKU overlay: Mem0 Starter **$3.80 / 1k**, Pro **$4.98 / 1k**, Hobby saturates at 1k retrieve-turns (lines 338–344). Write-path extract extra (2k in / 400 out ≈ **$0.008**; embed 500×200 tok **$0.002**) at lines 348. Cache assumption: pin 1–2k profile card in cached prefix; retrieve-k uncached (lines 352).
- Latency tiers with mitigations at §3.2 lines 354–381. Paper search/total p50/p95 (Mem0 **0.148 / 0.200** search, **0.708 / 1.440** total) at lines 356–367. **[inferred] policy targets** (not vendor SLOs; p99 unpublished, gap noted lines 371–373): **p50** retrieve Mem0 **148 ms** / Zep vendor **155–162 ms** / hot STM **<50–200 ms** (pin card; skip ANN for “just told you”; id-fetch after write); **p95** e2e Mem0 paper **1.44 s** vs stuff **17.1 s**; copilot retrieve **<300 ms [target, measure]** (constructor cap; async extract; fail open to STM); **p99** hang fail-**open** on search timeout **200–500 ms [inferred]** (watermark + episode fallback; breaker retrieve ≠ write ≠ embedder).
- Throughput and back-pressure at §3.3 lines 383–403: constructor cap 1.6k/~4k/~7k; Mem0 Starter **5k retrievals/mo**; Zep Flex **600 / 1000 RPM**; Store `asearch` `limit=10` silent truncate; Pinecone 1 RU/GB of **that** namespace; Letta **$0.00015/s** server-side tools; sleep-time every **5** steps; extract queue Temporal/Kafka. Admit read iff retrieve breaker ∈ {closed, half-open} **or** cached card to fail open; shed skip LTM → STM+card → skip compressor extras → still answer; writes queue/ack/idempotent; graph backpressure + watermark.

**3. NFR Trade-offs — PASS**
- Availability, RPO, RTO, compliance stated with working targets at §3.4 lines 405–416 (availability **99.9% gateway**; retrieve fail-**open** to STM+card; chat must not 500 on ANN timeout; multi-vendor LLM fallback for 503/529 on **generate** not extract; **RPO** STM=checkpointer, LTM=last **acked** extract, episodes=append-only transcript before extract, KV/prompt-cache minutes best-effort, `"exit"` durability is not a memory store; **RTO** retrieve timeout **< 500 ms** then STM, Art. 12(3) erasure **max one month** (+2 if notify), graph rebuild hours is **not** a caller RTO; compliance per-user keys/indexes, Art. 17 fan-out including HNSW compaction/Ghost Vectors, BAA where health, vectors = DLP class of chat logs, no training on raw personal memory).
- Competing-NFR tensions in the same table: failover **busts** prefix cache vs availability / never failover a 400 extract; extract in-flight crash → replay via idempotency not duplicate ADD; fast fail-open vs bit-identical personalization / sleep-time rewrite vs cache hit; STM linear vs LTM eventual / query-after-write ANN / orphan Store `put` vs rolled-back checkpoint; Graphiti invalidation **preserves** history vs Art. 17 **hard-delete** / Mem0 v3 deletion is a separate pipeline; constructor **~$11–21 / 1k** vs stuff **$59 / $237** vs LangMem 18–60 s / sleep-time without amortization; frozen card in cached prefix vs isolation / mid-session rewrite busts cache.

**4. Distributed Resilience — PASS**
- Durable execution: Temporal / Kafka **equivalent** at §4.1 lines 423–451 (STM checkpointer = transcript at super-step grain; episode append = non-lossy log; extract Workflow/Kafka consumer Activities redact → extract LLM → ACL stamp → embed → upsert with idempotency `(thread_id, checkpoint_id, memory_id)`; watermark topic; sleep-time separate workflow single-writer; **DLQ** poison extracts). Kafka/outbox mapping: `memory.intents`, `memory.episodes`, `memory.extracts`, `memory.upserts`, `memory.erasure`, `memory.dlq`; compaction on `user_id`; full log = chain-of-custody (line 447). Replay vs resume: extract Activity retry re-runs extract — must be idempotent (line 449). Searchable-after table (lines 437–445). Encoded in `ExtractUpsert` / idempotency map (§5).
- Failure taxonomy at §4.2 lines 453–468: **transient** / **permanent** / **poison pill (memory)** / **poison pill (ops)** / write-read race / stale / summarization loss / namespace leak / soft-delete “erasure” / embedder drift / judge overfitting / **orphan/ghost write**. Idempotency: key on extract (line 429); exactly-once via keys never shared txn (line 434); Activity retry must be idempotent (line 449); orphan/ghost handler (line 468). Poison recovery (lines 470): quarantine by origin; replay from episodes; hard-delete + HNSW compaction; no auto-promote web/tool.
- Circuit breaker closed → open → half-open at §4.2 lines 472–486 (one breaker per retrieve, write/extract, embedder, MCP memory server; do not open solely on 429-with-Retry-After; half-open probe is a **cheap read**, not `archival_memory_insert`; retrieve fail-**open**), including ASCII state diagram lines 474–486. Encoded in `BreakerStateMachine` (§5 lines 635–685).
- Fallback chain: LTM retrieve → (breaker open / timeout) → **STM + cached profile card** → (STM missing) → **deterministic** `{"status":"degraded","memory":"none"}`; `PermanentError` on RBAC/schema **does not** failover to shared index; `GraphRecursionError` is a fuse not a memory retry (line 488). Encoded in `deterministic_degraded` / retrieve fail-open (§5 lines 742–743, 1226–1227).

**5. Enterprise Security Boundaries — PASS**
- Zero-Trust MCP at §4.3 lines 490–502: memory tools are privilege; remote servers OAuth 2.1 resource servers; RFC 9728; **RFC 8707** `resource` indicator; PKCE; MUST validate audience; MUST NOT token-passthrough; `tenant_id`/`user_id` **only from verified token**; observation vs belief stores; `/memories` path-traversal deny; sleep-time = admin writer; re-validate at execution; dual-LLM quarantine of untrusted `tool_result`; `/mcp` stateless per request. Topology Zero-Trust box (lines 94–109). Code: `ZeroTrust` proxy rejects confused-deputy `user_id` in tool args (§5 line 1234).
- Tool-level RBAC / least privilege at §4.4 lines 504–514: map search vs insert vs delete vs `add_episode` vs compact; `(principal, tenant, tool, origin)` → allow / deny / HITL (request-flow line 153); namespace from **token**; write allow-list user-confirmed / sleep-time / never raw tool-result; resume values re-RBAC before a new `insert`. Encoded in `NamespaceACL` write_roles (§5 lines 778–798).
- PII pipeline detect → redact → audit at §4.5 line 532 (ingress **and** before embed **and** before trace; Vec2Text 92% exact on 32-token inputs at lines 518; PCI: no PAN in memory). Topology Policy “PII redact BEFORE embed” (lines 54–58); request-flow step 2 (line 153). Encoded in `redact_pii` (§5 lines 723–739).
- Immutable telemetry / chain-of-custody at §4.5 line 534 (WORM: `correlation_id`, tenant, hashed user, `memory_id`, `origin`, tool name, k, memory_ids that entered the prompt, constructor tokens, model/index versions, watermark offset, HITL actor extractor vs owner, breaker state, Art. 17 request id + physical-purge ids; reconstruct personalization as policy snapshot + packed memory_ids + hashed card + generate; provider traces are not a SIEM). Topology `Audit (WORM)` (§1.1 lines 114–120). Kafka full log as chain-of-custody (§4.1 line 447). Art. 17 fan-out including HNSW compaction / Ghost Vectors (lines 520–530).

**6. Design Case Studies — PASS**
- Exactly two scenarios in §6 (header at line 1244: “Exactly two enterprise designs”).
- **Scenario 1** (§6 lines 1246–1300): 10M MAU B2C copilot, Sonnet 5 constructor **~$11–21 / 1k** vs stuff **~$59 / ~$237 / 1k**, Mem0 meter **~$3.80–4.98 / 1k**, retrieve p95 **<300 ms [target]**, Art. 17 fan-out, no training on memories; ASCII architecture lines 1252–1286; 3-way matrix (A/B/C) across cost, latency, ops complexity, security, scalability lines 1292–1298; rationale recommends **B** (four planes, async extract, constructor ≤4k, ns=tenant, fail-open STM+card, nightly single-writer card) line 1300.
- **Scenario 2** (§6 lines 1302–1349): call-center after-call summaries, HIPAA/PCI, CRM authoritative, three parallel extractors, inbound brief not 90d dump, **[inferred] ~$480/day** extract + **~$1.92/day** embed at 80k calls; ASCII architecture lines 1308–1335; 3-way matrix lines 1341–1347; rationale recommends **B** (live STM + brief; after-call triple extract; quote-backed rows; transcript as episode; CRM SoR) line 1349.

### STATUS: APPROVED
