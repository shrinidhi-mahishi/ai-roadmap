## Module: 05-langgraph-state-machines.md

### Criteria Results
| # | Criterion                        | Verdict |
|---|----------------------------------|---------|
| 1 | ASCII System Topology            | ✅  |
| 2 | Token Cost Economics & SLA        | ✅  |
| 3 | NFR Trade-offs                   | ✅  |
| 4 | Distributed Resilience           | ✅  |
| 5 | Enterprise Security Boundaries   | ✅  |
| 6 | Design Case Studies              | ✅  |

### Deficiencies (if any)
None. All six criteria are met at the cited sections/line ranges below.

**1. ASCII System Topology — PASS**
- Diagram present with box-drawing characters (`┌─┐│└─┘├┤┬┴┼─`) at §1.1 lines 48–133 (not tables-only; plane table at lines 137–144 is accompanying).
- Planes mapped in the diagram: **Control** (lines 56–85: edge auth/SSO/25 MB→413/409 reject, policy PII redact + tool RBAC + `Runtime.context`, Pregel scheduler plan/execute/update, durability `exit|async|sync`, thread lease ≤1 run/`thread_id` + `N_JOBS=10`, graph orchestrator channels/`Command`/`Send`/HITL/`recursion_limit`/stream mux, circuit breaker LLM≠tool≠MCP, fallback primary→secondary→degraded, SIGTERM drain), **Data** generation + executor (lines 90–99: tokenizer → prefill → decode; tool JSON/draft/plan; `ToolNode` parallel `tool_calls`, InjectedState/Store/Runtime, `handle_tool_errors`, `remaining_steps < 2` wrap-up), **Tool proxies** (lines 104–118: Zero-Trust wrap, RFC 8707 audience, no token passthrough, identity = `langgraph_auth_user` / `Runtime.context`, Stripe/send HITL, CRM/MCP `tools/call` + SSRF filter, `/mcp` stateless), **Persistence** (lines 104–119: checkpointer `thread_id`/`checkpoint_ns`/pending writes/durability/time-travel, Store across threads, Postgres checkpoints+store + `EncryptedSerializer`, Redis pub/sub+queue **no user data**), **Telemetry** (lines 122–132: WORM audit cid/tenant/`thread_id`/`checkpoint_id`/node/hashed draft/HITL decision, super-step p50/p95 + Send width + 409 + `GraphRecursionError`/`InvalidUpdateError`, traces gateway→LLM→ToolNode→HITL with PII stripped, usage `total_cost_usd`/LCU/LSU/`langgraph_step` on terminal event).
- Request-flow narrative is §1.2 lines 148–161 (12-step ingress → policy+runtime context → admit/lease → Pregel plan → execute → update+checkpoint → route/`Send` → HITL → stream → Store vs checkpoint → halt+WORM → SIGTERM drain).

**2. Token Cost Economics & SLA — PASS**
- Explicit `$ per 1k` with assumptions at §3.1 lines 390–407 (and list-price skeleton at lines 382–388). Formula \(C_{\mathrm{call}} = (3000\,P_{\mathrm{in}} + 800\,P_{\mathrm{out}})/10^{6}\) (lines 384–386); GPT-4.1 \(C_{\mathrm{call}}=\$0.0124\). Assumptions: 3,000 input + 800 output per LLM call; ticket skeleton **3** LLM nodes; Send research **10** LLM calls (planner + N=8 workers + synthesizer); HITL wait **$0** model and **$0** worker while parked (line 388). Tables: ticket GPT-4.1 **$37.20 / 1k**, Luna **$2.10 / 1k**, GPT-5.4 **$58.50 / 1k** (lines 394–399); Send N=8 GPT-4.1 **$124 / 1k**, Luna **$7 / 1k**, GPT-5.4 **$195 / 1k** (lines 403–407). Platform compute **[inferred] ~$0.038–$0.04 / 1k** at 2 vCPU-s (lines 417, 19); Dedicated Small always-on **≈ $390/mo** (lines 419). Checkpoint growth / full-`messages` resubmit called out as a token problem (lines 411–412); cache-stable tools prefix deferred to 02 (line 435).
- Latency tiers with mitigations at §3.3 lines 431–437. **[inferred] policy targets** (not vendor SLOs; gap noted lines 423–425): **p50** ticket wall (pre-HITL) **3 LLM sequential**; wall **< 8 s** if retrieve is local (stream `messages` not `values`; cache-stable tools prefix; `durability="async"` on non-money ticks); **p95** Send research planner + **max(worker)** + synth; wall **< 20 s** if workers are I/O (cap `Send` width at 8; per-`Send` timeout; join on `operator.add`); **p99** hang fail-closed on node/Activity timeout / 409-storm (async `TimeoutPolicy`; `RemainingSteps` wrap-up; `reject` vs `enqueue`; `durability="sync"` only around send/refund). HITL p99 = human SLA.
- Throughput and back-pressure at §3.4 lines 441–461: knobs `recursion_limit` **1000**, `RemainingSteps`, `RetryPolicy`, `N_JOBS_PER_WORKER=10`, 1 run/thread → **409**, `Send` width, durability, 25 MB **413**. Admit iff LLM breaker ∈ {closed, half-open} **and** no executing run on the thread **and** `N_JOBS_PER_WORKER` has room. Shed: `reject` (409) → disable parallel writes → skip synthesizer extras → deterministic degraded JSON. Queue workers required if `queue.enabled`. Cap `Send` + inner `max_turns` / `ModelCallLimitMiddleware`. 100 concurrent tickets × 3 LLM nodes = **300** in-flight; Send N=8 on 20 jobs = **160** worker calls in one tick.

**3. NFR Trade-offs — PASS**
- Availability, RPO, RTO, compliance stated with working targets at §3.5 lines 464–474 (availability **99.9% gateway**; graph still ends on fuse; multi-vendor LLM fallback for 503/529; Agent Server `reject` → 409 is back-pressure not an outage; **RPO=0** for HITL-irreversible via `durability="sync"` + pending writes, Store TTL hours, KV/prompt-cache minutes best-effort, `"exit"` RPO = whole run, Redis **not** in the RPO story; **RTO** interactive LLM failover **< 1 s**, resume HITL with same `thread_id` + `Command(resume=...)`, in-flight payment **same** idempotency key, Temporal HITL is a **signal** with no worker CPU; compliance regional Cloud **US / EU** not migratable, ZDR/traces, checkpoints are **PII stores**, `EncryptedSerializer` + `LANGGRAPH_AES_KEY`, WORM HITL decisions).
- Competing-NFR tensions in the same table: failover **busts** prefix cache vs availability / never failover a 400; `"async"` races the next super-step / `InMemorySaver` RPO = process life; fast failover vs bit-identical tokens (T>0) vs node-restart replay of side effects; channels+reducers as DAG truth vs at-least-once token change / `Command`+static edge double-fire; residency vs latency vs DLP on unbounded `messages`; cost vs latency (ticket **$37.20/1k** vs Send N=8 **$124/1k** vs Luna **$2.10/1k**; Dedicated S **~$390/mo** vs serverless ~**$0.04/1k** compute; `durability="sync"` on every tick); cache hit rate vs tenant isolation / PDFs in state → 413.

**4. Distributed Resilience — PASS**
- Durable execution: Temporal / Kafka **equivalent** at §4.1 lines 479–504 (checkpointer = Workflow history at super-step / entrypoint grain; Agent Server thread lease = `workflow-id` uniqueness ≤1 run/`thread_id`; Redis queue+pub/sub = Kafka *wakeup* topic, **not** SoR; Postgres = the log; Temporal plugin: graph as Workflow, nodes as Activities, `interrupt()` only in Activity nodes, wait is a **signal**, Activity retry re-runs the whole node, nested SDK `max_retries=0`). Kafka/outbox mapping: `graph.intents`, `graph.supersteps`, `tool.results`, `hitl.decisions` → Signal, `graph.dlq`; compaction on `thread_id`; poison skip + alert after N handler crashes (lines 491). Replay vs resume vs Temporal table (lines 497–502). Exactly-once super-steps vs at-least-once nodes (lines 345).
- Failure taxonomy at §4.2 lines 507–517: **transient** / **permanent** / **poison pill (graph)** / **poison pill (ops)** / **semantic** / **soft fuse as success** / **fuse mismatch** / **state bloat**. Idempotency: same key on transient handler (line 510); node-restart side effects must be idempotent (line 289); hop hash ≠ Stripe idempotency key (line 525); RTO same key (line 469). Poison repeating N=3 identical failures / N=6 empty calls (lines 519–525).
- Circuit breaker closed → open → half-open at §4.2 lines 527–541 (one breaker per (provider, model) **and** per tool-class / MCP server; do not open solely on 429-with-Retry-After; half-open probe is a **cheap read**, not `send_email`), including ASCII state diagram lines 529–541. Encoded in `BreakerStateMachine` / offline test (§5 lines 675–728, 993–1011).
- Fallback chain: primary (GPT-4.1 / Sol) → secondary vendor (same IR) → **deterministic** `{"status":"degraded"}` (no charge, no email); tool-class open → queue/HITL, not a second processor; `PermanentError` on schema/RBAC/`InvalidUpdateError` **does not** failover; `GraphRecursionError` is a fuse not a retry (line 543). Encoded in `FallbackChain` / `deterministic_degraded` (§5 lines 761–795).

**5. Enterprise Security Boundaries — PASS**
- Zero-Trust MCP at §4.3 lines 545–559: Agent Server `/mcp` Streamable HTTP; **each request stateless**; consume via `MultiServerMCPClient` with per-user `Authorization` from `langgraph_auth_user`; remote servers as OAuth 2.1 resource servers; RFC 9728 metadata; **RFC 8707** `resource` indicator; PKCE; MUST validate audience; MUST NOT token-passthrough; gateway RFC 8693 exchange; pin manifests `hash(description+schema)`; re-validate authorization at execution from `runtime.context` / `langgraph_auth_user`; `tool_filter` / per-assistant allowlist; HITL on mutating tools; dual-LLM quarantine of untrusted `tool_result`. Topology Zero-Trust box (lines 104–108). Code: `ZeroTrustToolProxy` (§5 lines 798–814, 1016–1027).
- Tool-level RBAC / least privilege at §4.4 lines 561–565: map `(principal, tenant, tool, args_shape)` → allow / deny / HITL; Agent Server `@auth.authenticate` + `@auth.authorize`; allowlist per assistant; do not bind a shared PAT; resume values re-RBAC before a new tool call; mutating tools as policy gates (`interrupt()` before `ToolNode`, `HumanInTheLoopMiddleware`, `interrupt_before=["tools"]`); split draft and send. Ingress policy (line 151).
- PII pipeline detect → redact → audit at §4.4 line 567 (DLP at ingress **and** executor **before** checkpoint/trace; PII in `messages` **is** the checkpoint; `RemoveMessage`, Store TTL, `PIIMiddleware`, LangSmith LLM Gateway redaction, region US/EU; `EncryptedSerializer` + `LANGGRAPH_AES_KEY`; encryption does not change what is persisted). Topology Policy “PII redact” (lines 61–65); request-flow step 2 (line 151).
- Immutable telemetry / chain-of-custody at §4.4 line 569 (WORM: `correlation_id`, tenant, `thread_id`, `checkpoint_id`, `checkpoint_ns`, `langgraph_step`, node, `Send` width, hashed draft / hashed tool args, policy/HITL decision + actor, breaker state, `total_cost_usd`, model id, durability mode; reconstruct a send as policy snapshot + sampled draft + hashed body + human interrupt + ToolNode result; Kafka full log as second copy; provider traces are not a SIEM; PostgresSaver **is** a PII store). Topology `Audit (WORM)` (§1.1 lines 124–131). Kafka compaction vs full log as chain-of-custody (§4.1 line 491).

**6. Design Case Studies — PASS**
- Exactly two scenarios in §6 (header at line 1132: “Exactly two enterprise designs”).
- **Scenario 1** (§6 lines 1134–1187): multi-tenant ticket HITL, 1k tickets/day, GPT-4.1 **~$37.20 / 1k** + **~$0.04 / 1k** serverless compute, Dedicated S ~**$390/mo**, UUID `thread_id`, `durability="sync"` on send, no API keys in state; ASCII architecture lines 1140–1173; 3-way matrix (A/B/C) across cost, latency, ops complexity, security, scalability lines 1177–1185; rationale recommends **B** (Postgres / Agent Server + `interrupt()` on `review` + `durability="sync"` + UUID `thread_id` + ToolNode wrap) line 1187.
- **Scenario 2** (§6 lines 1189–1234): `Send` research fan-out N=8, GPT-4.1 **~$124 / 1k**, latency ≈ planner + max(worker) + synthesizer, width cap in the router, `operator.add` on `findings`; ASCII architecture lines 1195–1220; 3-way matrix lines 1224–1232; rationale recommends **B** (`Send` fan-out N=8 + reducer + width cap + inner worker fuses) line 1234.

### STATUS: APPROVED
