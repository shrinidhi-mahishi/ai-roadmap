## Module: 04-agent-loop-patterns.md

### Criteria Results
| # | Criterion                        | Verdict |
|---|----------------------------------|---------|
| 1 | ASCII System Topology            | ✅  |
| 2 | Token Cost Economics & SLA        | ✅  |
| 3 | NFR Trade-offs                   | ✅  |
| 4 | Distributed Resilience           | ✅  |
| 5 | Enterprise Security Boundaries   | ✅  |
| 6 | Design Case Studies              | ✅  |

### Deficiencies (if any)
None. All six criteria are met at the cited sections/line ranges below.

**1. ASCII System Topology — PASS**
- Diagram present with box-drawing characters (`┌─┐│└─┘├┤┬┴┼─`) at §1.1 lines 44–131 (not tables-only; plane table at lines 133–142 is accompanying).
- Planes mapped in the diagram: **Control** (lines 52–83: edge auth/RPM/breaker, policy PII+tool RBAC+frozen goal, loop controller ReAct|P&E|Reflection, hop counter `max_turns`/`recursion_limit`/`remaining_steps`/`max_budget_usd`, hash circuit, Temporal orchestrator, checkpointer, circuit breaker, fallback), **Data** generation + executor (lines 88–98: tokenizer → prefill → decode; thought/tool JSON; `pause_turn` vs `end_turn`; complete-JSON execute-gate, `remaining_steps < 2` wrap-up, hosted/server-tool inversion), **Tool proxies** (lines 103–114: Zero-Trust RFC 8707, no token passthrough, identity from session JWT / Temporal info, Stripe refund HITL, CRM/MCP `tools/call` with SSRF filter), **Persistence** (lines 103–117: checkpointer `thread_id`/plan/`past_steps`/HITL/pending writes, Reflexion TTL store, idempotency saga/outbox/Temporal history, soft prompt-cache KV), **Telemetry** (lines 119–130: WORM audit cid/tenant/hop/pattern/SHA-256 args/frozen_goal/stop_reason/HITL, hop p50/p95 metrics, traces gateway→LLM→executor→HITL, usage `total_cost_usd`/`num_turns` on terminal event).
- Request-flow narrative is §1.2 lines 146–159 (12-step ingress → policy+frozen goal → pattern dispatch → hop/dollar/super-step fuses → checkpointer → model Activity → parse `stop_reason` → execute-gate → observation-as-data → critic → replan/join → emit+audit).

**2. Token Cost Economics & SLA — PASS**
- Explicit `$ per 1k` with assumptions at §3.1 lines 314–331. Formula \(C = n \cdot (T_{\mathrm{miss}} P_{\mathrm{miss}} + T_{\mathrm{hit}} P_{\mathrm{hit}} + T_{\mathrm{write}} P_{\mathrm{write}} + T_{\mathrm{out}} P_{\mathrm{out}}) / 10^{6}\) (lines 308–312); \(T_{\mathrm{out}}\) includes thinking. Assumptions: GPT-6-sol short-context ($2 in / $0.20 cached / $2.50 cache write / $10 out); 8k frozen prefix, 400 output tokens/turn, 600 new input tokens/turn, cache hit on prefix from turn 2 (line 316). Table (lines 318–323): 1 hop **$24 / 1k**; 3 hops **$38 / 1k**; 10 hops (Agents SDK default) **$85 / 1k**; 25 hops **$187 / 1k**. Uncached-prefix 10-hop **~$200/1k** (line 325). Mix 1k conversations/day **~$31/day** vs 25-hop runaway **~$187/day (~6×)** (line 329). Self-Refine **$108/1k** vs one-shot **$12/1k** (line 327). ReWOO/LLMCompiler published dollars cited (lines 197–200, 331).
- Latency tiers with mitigations at §3.2 lines 333–356. Paper means and caps at lines 337–344. **[inferred] policy targets** (lines 349–354): **p50** hops/wall (support ReAct) **1–3 hops**; wall **< 8 s** (parallel independent reads in one turn; cache-stable tools prefix; `max_turns=8–10`; REST tool p95 **<800 ms**); **p95** **≤ 8 hops**; wall **< 20 s** unless HITL (hash circuit N=3; `RemainingSteps` wrap-up; no Self-Refine on the tail; P&E when ≥3 independent searches); **p99** hang fail-closed on stall watchdog / Activity timeout (nested timeouts ordered; one retry owner; hash circuit; `max_budget_usd`; HITL p99 = human SLA).
- Throughput and back-pressure at §3.3 lines 358–367: 100 concurrent tickets × 10 turns = **1,000** in-flight model calls if uncapped; web search **$10/1k**. Admit iff LLM breaker ∈ {closed, half-open} **and** hop budget remaining **and** tool-class semaphore has room. Shed order: disable parallel writes → skip critic → force P&E join → deterministic degraded JSON. Partition `prompt_cache_key` by tenant; batch worker pool sized to tool RPM; inner ReAct `max_turns=3` per plan node.

**3. NFR Trade-offs — PASS**
- Availability, RPO, RTO, compliance stated with working targets at §3.4 lines 369–379 (availability **99.9% gateway**; loop still ends on fuse; multi-vendor LLM fallback for 503/529, never failover a 400; **RPO=0** for HITL-irreversible via `sync` checkpointer + frozen goal + pending writes, Reflexion TTL hours, KV/prompt-cache minutes best-effort; **RTO** interactive LLM failover **< 1 s**, resume HITL with same `thread_id` + `Command(resume=...)`, in-flight payment **same** idempotency key; compliance regional +10% / Anthropic 1.1× US geo, ZDR excludes batches/files, `trace_include_sensitive_data=false`, checkpoints are PII stores).
- Competing-NFR tensions in the same table: failover **busts** prefix cache vs availability; `"async"` durability races the next super-step / `"exit"` RPO = whole run; fast failover vs bit-identical tokens (T>0) vs node-restart replay of side effects; plan/`past_steps` as DAG truth vs at-least-once token change; residency vs latency vs DLP on 25-hop transcripts; cost vs latency (cached 10-hop **$85/1k** vs 25-hop **$187/1k** vs uncached 10-hop **~$200/1k** vs Self-Refine **$108/1k**); cache hit rate vs tenant isolation / compaction rewrite miss storm.

**4. Distributed Resilience — PASS**
- Durable execution: Temporal (LangGraph cognition inside Temporal/Inngest; every LLM call and tool I/O is an Activity; Workflow = deterministic loop; `max_retries=0` in SDK; `workflow-id = tenant:thread_id`; checkpointer at super-step boundaries; durability `"sync"`/`"async"`/`"exit"`; pending writes keep successful siblings; crash mid-hop restarts the node from the top) and Kafka (`loop.intents` before first model call, `loop.hops`, `tool.results`, `hitl.decisions` → Signal the Workflow, `loop.dlq`; compaction on `thread_id`; poison skip + alert after N handler crashes) at §4.1 lines 385–397.
- Failure taxonomy at §4.3 lines 405–413: **transient** / **permanent** / **poison pill (loop)** / **semantic** / **soft fuse as success** / **fuse mismatch** / **critic regression**. Poison repeating circuit N=3 / N=6 / oscillation window at lines 415–421. Idempotency: hop hash ≠ Stripe key; derive money keys from `call_id` / workflow ids (line 423); saga/outbox/Temporal history in topology (lines 113–115); RTO same key (line 375); invariant 10 (line 302).
- Circuit breaker closed → open → half-open at §4.3 lines 425–438 (one breaker per (provider, model) **and** per tool-class; do not trip on 429-with-Retry-After; half-open probe is a **cheap read**, not `issue_refund`), including ASCII state diagram lines 427–438. Encoded in `BreakerStateMachine` / offline test (§5 lines 574–632, 1129–1146).
- Fallback chain: primary (Sonnet 5 / GPT-6-sol) → secondary vendor (same IR) → **deterministic** `{"status":"degraded"}` (no charge, no email); tool-class open → queue/HITL, not a second processor; PermanentError on schema/RBAC **does not** failover (line 441). Encoded in `FallbackChain` / `deterministic_degraded` (§5 lines 864–898).

**5. Enterprise Security Boundaries — PASS**
- Zero-Trust MCP at §4.4 lines 443–447: remote servers as OAuth 2.1 resource servers; RFC 9728 metadata; **RFC 8707** `resource` indicator; PKCE; MUST validate audience; MUST NOT token-passthrough; gateway RFC 8693 exchange; pin manifests `hash(description+schema)` against rug-pulls; re-validate authorization at execution; DNS-rebinding CVE-2025-66414 / 66416 with pin TS ≥1.24.0 / Python ≥1.23.0; dual-LLM quarantine of untrusted `tool_result`. Topology Zero-Trust box (lines 103–108).
- Tool-level RBAC / least privilege at §4.4 line 449: identity from session JWT / Temporal memo / MCP access token, **never** model JSON; map `(principal, tenant, tool, args_shape)` → allow / deny / HITL; one named tool per P&E step; `allowed_tools` per hop so observation cannot enable `shell`; schema-constrained plan JSON; reject unknown names before executor. HITL on irreversible tools (line 451). Ingress policy (line 149). Code: `_run_tools` allowlist + irreversible HITL (§5 lines 975–1003).
- PII pipeline detect → redact → audit at §4.4 line 453 (DLP at ingress **and** executor **before** checkpoint/trace; `trace_include_sensitive_data` default **true** unless set false; LangSmith / PostgresSaver store full state; Reflexion/Self-Refine histories as durable injection + PII; stable placeholders; WORM placeholder → **hash**, not plaintext). Topology Policy “PII redact” (lines 56–62); request-flow step 2 (line 149).
- Immutable telemetry / chain-of-custody at §4.4 line 455 (WORM: `correlation_id`, tenant, `thread_id`, hop, pattern, `stop_reason`, frozen-goal hash, tool name, `call_id`, hashed args, policy/HITL decision, hash-circuit count, breaker state, `total_cost_usd`/`num_turns`, model id; reconstruct a refund as policy snapshot + frozen goal + sampled tool_use + hashed args + Stripe status + human interrupt; Kafka full log as second copy; provider traces are not a SIEM). Topology `Audit (WORM)` (§1.1 lines 121–128). Kafka compaction vs full log as chain-of-custody (§4.1 line 395).

**6. Design Case Studies — PASS**
- Exactly two scenarios in §6 (header at line 1261: “Exactly two enterprise designs”).
- **Scenario 1** (§6 lines 1263–1315): customer-support ReAct, 1k conversations/day, peak 100 concurrent, mix **~$31/day** vs 25-hop runaway **~$187/day**, HITL on refund/email, `trace_include_sensitive_data=false`; ASCII architecture lines 1269–1301; 3-way matrix (A/B/C) across cost, latency, ops complexity, security, scalability lines 1307–1314; rationale recommends **B** (ReAct `max_turns=8–10`, remaining-steps wrap-up, hash N=3, critic off, HITL money) line 1315.
- **Scenario 2** (§6 lines 1317–1365): batch plan-and-execute research, 8+ independent searches, `max_replans=2`, inner `max_turns=3` + single-tool allowlist, `max_budget_usd` $2–5/job; ASCII architecture lines 1323–1350; 3-way matrix lines 1356–1363; rationale recommends **B** (P&E/DAG + inner ReAct `max_turns=3` + `max_replans=2` + worker fuses) line 1365.

### STATUS: APPROVED
