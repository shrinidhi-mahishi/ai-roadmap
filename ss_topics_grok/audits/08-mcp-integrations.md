## Module: 08-mcp-integrations.md

### Criteria Results
| # | Criterion                        | Verdict |
|---|----------------------------------|---------|
| 1 | ASCII System Topology            | ✅  |
| 2 | Token Cost Economics & SLA        | ✅  |
| 3 | NFR Trade-offs                   | ✅  |
| 4 | Distributed Resilience           | ✅  |
| 5 | Enterprise Security Boundaries   | ✅  |
| 6 | Design Case Studies              | ✅  |

### Deficiencies (if any)
None. All six criteria are met at the cited sections/line ranges below.

**1. ASCII System Topology — PASS**
- Diagram present with box-drawing characters (`┌─┐│└─┘├┤┬┴┼─`) at §1.1 lines 48–141 (not tables-only; plane table at lines 145–152 and host/client/server table at lines 156–160 are accompanying).
- Planes mapped in the diagram: **Control** (lines 57–91: HOST UX/consent/HITL, CLIENT mapping, Edge SSO/EMA/CIMD+PKCE/Origin 403/bind 127.0.0.1, Policy PII redact before resources enter the model, Catalog `tools/list` ALL pages/`nextCursor`/hash pin, Handshake `server/discover` vs legacy init, Headers `MCP-Protocol-Version`/`Mcp-Method`/`Mcp-Name`, GATEWAY PEP RFC 8707 `aud` + body-free authorize + no inbound-token passthrough + `cacheScope=private`, Circuit ONE per MCP server, Fallback primary→secondary→degraded, SIGTERM drain close SSE / persist taskId), **Data** MCP JSON-RPC + upstream/webhook (lines 96–107: `tools/call`/`resources/read`/`prompts/get`, `resultType` complete|input_required|task, `isError=true` vs −32602, `subscriptions/listen`; OpenAPI/Smithy/Lambda/first-party MCP, NEW outbound token, Stripe-Signature HMAC on RAW body, UNIQUE `event.id`, 2xx before accounting, reconciler GET), **Tool proxies** (lines 110–124: Zero-Trust wrap, RFC 8707 `aud`, tenant NEVER from tool args, stdio env creds vs Streamable HTTP POST-only 2026, legacy GET-SSE, OpenAPI→MCP one op → 1 tool, code-mode search+execute), **Persistence** (lines 111–125: TASK STORE taskId/ttlMs/pollInterval, CATALOG cache ttlMs/hash pin/`list_changed`, WEBHOOK `event.id` UNIQUE, TOKEN VAULT not `mcp.json`, handles in tool args), **Telemetry** (lines 127–140: WORM cid/tenant/`Mcp-Method`/`Mcp-Name`/catalog hash/taskId/`event.id`/`aud`/no raw Bearer; metrics `tools/call` p50/p95/p99 [policy]/page drain/breaker per server/webhook 2xx; traces gateway→PEP→server→upstream with `traceparent` in `_meta` and PII stripped; usage schema tok/result tok/MCP SKU=$0/`total_cost_usd`/OpenAI MCP RPM).
- Request-flow narrative is §1.2 lines 164–186 (interactive path 1–10: ingress token-bound ids → PEP RFC 8707 + `Mcp-Name` → handshake `server/discover` then legacy init → catalog drain every `nextCursor` → map to native schema → model native `tool_use` → execute with minted outbound token → MRTR/Tasks branch → cancel/drain → halt+WORM; webhook dual-path 11–14: `create_*` persists `(taskId, upstream_id, tenant)` → HMAC + UNIQUE `event.id` + 2xx → worker completes task row → reconciler GET if webhook lost).

**2. Token Cost Economics & SLA — PASS**
- Explicit `$ per 1k` with assumptions at §3.1 lines 328–356 (stated MCP-using question = 1k user questions; each = **1 planning model-call** + **1 host-side JSON-RPC `tools/call`** + **1 synthesis model-call**; no thinking; generator Claude Sonnet 5 **$2 / $0.20 cached / $10 out** per MTok; MCP protocol fee **$0**; catalogs Anthropic five-server **55k** or tool-search **~8.7k**; hidden tool-use prompt **354** on top when inlined). Protocol line: MCP SKU **$0**; 1k results @ 800 tok × $2/1e6 = **$1.60 / 1k** input (lines 332–338). Catalog tax tables (lines 340–354): 55k uncached 2 turns **≈ $228 / 1k**; 55k cache-read **≈ $30 / 1k**; 8.7k cache-read **≈ $11.4 / 1k** + result tokens. 1k-call overlay: ~0.4 MTok in ≈ **$0.08** cached vs **$0.80** if 55k uncached every turn (line 356). Cursor **−46.9%** scoped to MCP-calling traces, not list-price $/1k (line 358).
- Latency tiers with mitigations at §3.2 lines 378–387. **[inferred policy]** (not vendor SLOs; gap noted lines 324–326, 421): **p50** MCP hop **80–250 ms** remote / **<50 ms** local stdio (persistent HTTP pool; cached `tools/list`; skip hosted list when `allowed_tools` pinned); **p95** MCP hop **400 ms–2 s** (cap upstream; `X-Accel-Buffering: no`; do not wait on HITL inside the hop); **p99** MCP hop **fail closed at 2–8 s** per server (per-server breaker; do not hold SSE through a 60 s gateway); **p50** e2e **0.8–3 s**; **p95** e2e **2–8 s** (progressive discovery; honor `pollIntervalMs` off user-facing SSE); **p99** e2e **8–15 s** with hop cap (wall-clock; degraded JSON; unbounded poll / 50-retry poison has **no** p99). Qualitative framing lines 364–376: no published MCP percentiles; framing **1–5 ms** is not the SLO.
- Throughput and back-pressure at §3.3 lines 389–407: OpenAI Responses MCP RPM Tier 1 **200** / Tiers 2–3 **1000** / Tiers 4–5 **2000** (independent of model RPM); Anthropic hosted RPM unpublished, cap **20** servers; AgentCore **30**/page; Cloudflare `maxSubscriptions` **1,024** / keep-alive **15 s**; Stripe webhook retry **3 days**. Admit the turn iff per-server breaker ∈ {closed, half-open} **or** fallback/degraded; shed skip HITL on reads → progressive discovery → fail that server (keep others) → deterministic `status: "degraded"`; never 50-retry a poison server; size MCP RPM separately; honor `pollIntervalMs`; `list_changed` re-list **once** from cursor=null.

**3. NFR Trade-offs — PASS**
- Availability, RPO, RTO, compliance stated with working targets at §3.4 lines 409–419 (availability **99.9% gateway [policy]**; spec defines error mapping not 99.9%; Cursor isolates failed MCP call; per-server breaker, never one global; **RPO** interactive=last acked JSON-RPC result (in-flight SSE lost on 2026 reconnect — no `Last-Event-ID`), Tasks=last persisted `taskId` + store write, webhooks=last UNIQUE `event.id` with Stripe **3-day** retry if not 2xx, catalog cache `ttlMs` best-effort; **RTO** stateless 2026 retry POST on any replica, stdio restart (in-flight lost), interactive timeout **2–8 s [policy]** then breaker, long-job webhook RTO **days** / Tasks poll **seconds** after store update; compliance RFC 8707 audience, **no passthrough**, EMA/CIMD, Origin 403 + SDK ≥ 1.24.0, WORM of `Mcp-Name`+catalog hash+taskId+`event.id`, DLP on `resources/read` before the model, Anthropic connector **ZDR not eligible**, ZDR/data-residency stops at the MCP hop).
- Competing-NFR tensions in the same table: failover **busts** prefix cache vs availability / never failover a 400 schema or −32602 invalid cursor; listen-stream completeness vs agent-process lifetime; 2xx-before-write vs split-brain; fast degraded JSON vs bit-identical tool result; sticky 2025 sessions vs 2026 round-robin; hosted MCP vs VPC-only data / form elicitation as a PII pipe; cost vs latency (**~$11.4 / 1k** vs **~$30** vs **~$228** + **$0** MCP SKU; `require_approval: never` cuts p50 and **raises** blast radius); cache vs tenancy (`cacheScope: public` never on per-user tools; `list_changed` must bust MCP ttl **and** LLM prefix).

**4. Distributed Resilience — PASS**
- Durable execution: Temporal / Kafka **equivalent** at §4.1 lines 427–452 (Create Activity = `tools/call` `create_*` writes `(taskId, upstream_id, tenant, idempotency_key)` **before** `resultType: "task"`; webhook topic HMAC on raw body → UNIQUE `event.id` → 2xx → enqueue; Poll/Signal = `tasks/get` / `notifications/tasks`, never listen for completion; Reconciler GET by stored id; Cancel = `tasks/cancel` **and** upstream cancel; **DLQ** poison servers / unsigned webhooks / split-brain). Kafka/outbox mapping: `mcp.intents`, `mcp.results`, `saas.webhooks`, `mcp.dlq`, `mcp.catalog_hash`; compaction on `taskId`; full log = chain-of-custody (line 438). Replay vs resume: JSON-RPC retries are **at-least-once**; protocol has **no** idempotency key; mutating tools map duplicates to the original result (line 440). Durable-after table (lines 446–452).
- Failure taxonomy at §4.2 lines 456–470: **transient** / **permanent** / **poison pill (server)** / **poison pill (ops)** / confused deputy (OAuth + tool) / DNS rebinding / pagination dropout / webhook-Task split-brain / at-least-once double charge / oversized resource / header injection / sampling deprecation trap. Idempotency: required tool argument minted by the session (line 431); store includes 500s (line 468); Activity retry of `create_*` must hit the same store key (line 440). Poison recovery (line 472): isolate server; hash-pin; HITL on `list_changed`; never mix unvetted marketplace with secrets-bearing server; DLQ repeating −32603; do not auto-promote tool output URLs into `resources/read`.
- Circuit breaker closed → open → half-open at §4.2 lines 474–488 (one breaker per MCP server **plus** per upstream class **plus** hosted connector; do not open solely on 429-with-Retry-After; do not open GitHub because Stripe 429d; half-open probe is a **cheap read** / `tools/list` page 1 or canary `tasks/get`, not `create_checkout`), including ASCII state diagram lines 476–488. Encoded in `BreakerStateMachine` (§5 lines 691–725) and offline test (§5 lines 1152–1163).
- Fallback chain: primary MCP server (audience-bound) → secondary **equivalent** server (same compiled tool IR, **new** `aud`) → **deterministic** schema-valid decline (`status: "degraded"`, no charge, no email); server-class open → queue/HITL, **not** a second processor; `PermanentError` on `aud` / RBAC / −32602 invalid cursor **does not** failover to an unauthenticated server or token passthrough (line 490). Encoded in `FallbackHost` / `deterministic_degraded` (§5 lines 1022–1055).

**5. Enterprise Security Boundaries — PASS**
- Zero-Trust MCP at §4.3 lines 492–546: never trust catalog, resource body, annotation, token audience, or peer `cacheScope`; NIST SP 800-207 PEP/PDP; `Mcp-Name` is the action name. RFC 8707 audience is the ZT hinge (line 504); **no passthrough** (lines 506–511); confused-deputy OAuth-proxy and tool-authority species (lines 513–517); CVE-2025-66414 SDK ≥ 1.24.0 / bind `127.0.0.1` (line 519); EMA ID-JAG (line 523); SSRF on OAuth discovery (line 525); supply-chain hash-pin (line 527); sandbox (line 529); checklist identity / per-request authz / audience / no passthrough / least-privilege catalogs / egress / pin / poisoned-catalog assumption / telemetry / revocation (lines 531–544). Topology Zero-Trust box (lines 110–113). Encoded in `assert_audience` / `mint_outbound` / `GatewayPep` (§5 lines 777–956).
- Tool-level RBAC / least privilege at §4.4 lines 548–571: `(principal, tenant, method, name)` via `Mcp-Method`/`Mcp-Name` headers without body parse; map `issues_read` / `create_checkout` / `resources/read` on `hr://*` / `send_email`; identity from token, never tool args; resume/`requestState` re-RBAC; isolation ladder OAuth scope → gateway `Mcp-Name` allowlist → separate MCP servers → separate hosts/conversations. Encoded in `GatewayPep.authorize` payments cannot call `issues_read` (§5 lines 929–939, 1118–1122).
- PII pipeline detect → redact → audit at §4.5 lines 573–577 (ingress **and** before `resources/read` is injected **and** before trace; DLP after tool result before next model turn; PCI: no PAN in resources). Topology Policy “PII redact BEFORE resources enter the model” (lines 64–69); request-flow step 1–2 (lines 168–169). Encoded in `redact_pii` (§5 lines 755–756, 1167).
- Immutable telemetry / chain-of-custody at §4.5 lines 579–581 (WORM: `correlation_id`, tenant, hashed user, `Mcp-Method`, `Mcp-Name`, catalog hash, JSON-RPC id, `isError` vs protocol code, `taskId`, webhook `event.id`, inbound/outbound **audience** never raw Bearer, breaker state, protocol version; reconstruct a charge as policy snapshot + native `tool_use` + hashed args + MCP `tools/call` id + Stripe status + webhook id; provider traces are not a SIEM). Topology `Audit (WORM)` (§1.1 lines 130–138). Kafka full log as chain-of-custody (§4.1 line 438).

**6. Design Case Studies — PASS**
- Exactly two scenarios in §6 (header at line 1205: “Exactly two enterprise designs”).
- **Scenario 1** (§6 lines 1207–1262): enterprise MCP gateway PEP, 50 servers / thousands of tools / per-tenant OAuth / SOC2, 8.7k live defs cache-read **[inferred] $1.74/day** vs 55k uncached **$110/day**, extra hop **5–30 ms**, RFC 8707 `aud`=one gateway URI, no passthrough, unknown-tool after drain ≈ **0**; ASCII architecture lines 1213–1248; 3-way matrix (A/B/C) across cost, latency, ops complexity, security, scalability lines 1254–1260; rationale recommends **B** (gateway PEP, drain all pages, progressive discovery, hash-pin, per-server breaker) line 1262.
- **Scenario 2** (§6 lines 1264–1311): SaaS webhook + MCP Tasks hybrid (payments/CI/video render), public Streamable HTTP `2026-07-28` + OAuth 2.1, four-tool catalog, OpenAI MCP RPM **200–2000**, Anthropic ZDR ineligible; ASCII architecture lines 1269–1297; 3-way matrix lines 1303–1309; rationale recommends **B** (webhook + Tasks dual-path, HMAC raw body, UNIQUE `event.id`, 2xx then enqueue, poll `tasks/get`, reconcile from Stripe API) line 1311.

### STATUS: APPROVED
